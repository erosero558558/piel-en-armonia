# S13-02

## Resumen

- Se reforzo `bin/gen-sitemap.js` para generar `sitemap.xml` con base canonica `https://pielarmonia.com`.
- El generador ahora mezcla rutas publicas desde `es/**`, `en/**`, Astro, contenido JSON, catalogo comercial y rutas de contrato legacy necesarias para SEO.
- Se agrego cobertura explicita para la familia canonicamente exigida de `depilacion-laser`, que no estaba representada en `data/catalog/services.json`.
- Se anadio gate especifico para `S13-02` en `bin/lib/gate-checks.js` y test focal en `tests-node/gate-task-checks.test.js`.

## Verificacion

- `node bin/gen-sitemap.js`
- `git diff --check`
- `node --test --test-name-pattern="S13-02" tests-node/gate-task-checks.test.js`
- `node bin/gate.js S13-02`
- `npx playwright test tests/public-seo-routing.spec.js --grep "sitemap" --workers=1`

## Resultado

- `sitemap.xml` regenerado con `76` URLs.
- La spec SEO focal del sitemap quedo en verde (`3 passed`).
- El gate de `S13-02` pasa y confirma cobertura de paquetes, booking, portal, pricing y el servicio SEO canonico faltante.
