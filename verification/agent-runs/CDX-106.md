# CDX-106

## Resumen

- Slice `codex_frontend` del frente
  `STRAT-2026-03-turnero-web-pilot-multi-clinic-local` para cerrar
  `multi_clinic_local_readiness`.
- Corrige el renderer del piloto `admin-queue` para que no dependa de un DOM
  completo cuando los tests Node montan stubs sin `querySelectorAll()` ni
  `HTMLDetailsElement`.
- Agrega una lectura compacta visible del `control tower` multi-clinica en el
  modo diario/basic, sin mover la consola avanzada ni alterar el orden de sus
  hosts pesados.

## Write Set

- `src/apps/admin-v3/shared/modules/queue/render/section/install-hub/pilot/render.js`
- `src/apps/queue-shared/turnero-release-control-tower.js`
- `admin.js`
- `verification/agent-runs/CDX-106.md`

## Cambios aplicados

- `renderQueueOpsPilotView()` ahora consulta grupos `details` solo si el host
  expone `querySelectorAll()`, y degrada a `[]` en stubs minimos de Node.
- La deteccion de grupos abiertos deja de depender exclusivamente de
  `HTMLDetailsElement`; usa un guard compatible con entornos sin ese
  constructor global.
- Se incorpora `#queueOpsPilotCompactControlTowerHost` en la columna visible
  del piloto, entre `Rutas por clinica` y `Validacion operativa`.
- `turnero-release-control-tower.js` soporta una variante `compact` que reuse
  el mismo modelo multi-clinica y las mismas acciones de copia/JSON, mientras
  conserva intacta la tarjeta completa del host avanzado.
- La consola pesada sigue dentro de `queueOpsPilotAdvancedGroup` y mantiene el
  orden canonico:
    - `queueOpsPilotRolloutGovernorHost`
    - `queueOpsPilotExecutivePortfolioStudioHost`
    - `queueOpsPilotStrategyDigitalTwinStudioHost`
    - `queueMultiClinicControlTowerHost`
    - `queueReleaseBoardOpsHubHost`
    - `queueReleaseOpsConsoleHost`
    - `queueSurfaceAcceptanceConsoleHost`
    - `queueReleaseMissionControlHost`

## Validacion

```bash
node --test tests-node/turnero-release-ops-console.test.js
node --test tests-node/turnero-release-multi-clinic-control-tower.test.js
npm run test:turnero:web-pilot:contracts
npm run test:turnero:web-pilot:php-contract
TEST_LOCAL_SERVER_PORT=9248 TEST_REUSE_EXISTING_SERVER=0 npx playwright test tests/admin-queue-ops-hub.spec.js tests/admin-queue-pilot-governance.spec.js --workers=1
npm run test:turnero:web-pilot:ui
```

Resultado:

- `tests-node/turnero-release-ops-console.test.js`: `3 passed`.
- `tests-node/turnero-release-multi-clinic-control-tower.test.js`: `2 passed`.
- `npm run test:turnero:web-pilot:contracts`: `186/186` verde.
- `npm run test:turnero:web-pilot:php-contract`: verde.
- Playwright focal `admin-queue-ops-hub` + `admin-queue-pilot-governance`:
  `19 passed`.
- `npm run test:turnero:web-pilot:ui`: verde con reporte persistido en
  `verification/turnero-web-pilot/ui-report.json` y
  `verification/turnero-web-pilot/ui-report.md`.

- required_check: test:turnero:web-pilot:contracts | state: green | command: npm run test:turnero:web-pilot:contracts
- required_check: test:turnero:web-pilot:php-contract | state: green | command: npm run test:turnero:web-pilot:php-contract
- required_check: test:turnero:web-pilot:ui | state: green | command: npm run test:turnero:web-pilot:ui

## Notas

- `js/queue-operator.js` tambien cambia en local al regenerar el runtime, pero
  este closeout se mantiene centrado en el write set aprobado de `CDX-106`.
- El siguiente paso canonico es pasar la slice a `review`, refrescar
  `verification/focus-checks/FOCUS-2026-03-turnero-web-pilot-multi-clinic-cut-1.json`
  desde esta evidencia y ejecutar el `close`.
