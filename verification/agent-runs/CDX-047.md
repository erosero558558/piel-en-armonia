# CDX-047 - Public V6 ES required checks evidence resolver

Fecha: 2026-03-28
Owner: Ernesto
Executor: codex
Lane: `codex_transversal`
Subfrente: `SF-transversal-public-v6-es-support`

## Objetivo

Cerrar `copy_contract_validation` del foco `Public V6 ES` sin tocar copy ni runtime, resolviendo los required checks del foco desde la evidencia canonica de las slices frontend ya validadas.

## Contrato aplicado

- El foco mantiene exactamente estos required checks: `content:public-v6:validate`, `audit:public-v6:copy`, `test:frontend:qa:v6`.
- La fuente de verdad pasa a ser la evidencia versionada de `CDX-045` y `CDX-048`, ambas alineadas al subfrente `SF-frontend-public-v6-es-copy`.
- Cada evidencia expone lineas machine-readable `required_check` con `state: green|red` y el comando ejecutado.
- El resolver del orquestador usa la evidencia mas reciente del foco activo; historial fuera del foco no cuenta.
- `public_main_sync` queda explicitamente fuera de alcance de esta slice.

## Superficies tocadas

- `AGENT_BOARD.yaml`
- `PLAN_MAESTRO_CODEX_2026.md`
- `verification/agent-runs/CDX-045.md`
- `verification/agent-runs/CDX-048.md`
- `tools/agent-orchestrator/domain/focus.js`
- `tests-node/orchestrator/focus.test.js`
- `tests-node/orchestrator/diagnostics.test.js`

## Validacion

Checks corridos durante el cierre de la slice:

```bash
node --test tests-node/orchestrator/focus.test.js
node --test tests-node/orchestrator/diagnostics.test.js
node --test tests-node/close-command.test.js
node --test tests-node/publish-checkpoint-command.test.js
node agent-orchestrator.js status --json
node agent-orchestrator.js codex-check --json
node agent-orchestrator.js board doctor --json --profile ci
node agent-orchestrator.js handoffs lint --json
```

Resultado esperado:

- desaparece `warn.focus.required_check_unverified`
- los required checks del foco quedan resueltos desde `CDX-045` y `CDX-048`
- el foco avanza a `publish_readiness_review`
- el warning externo de `public_main_sync` puede permanecer sin bloquear el frente

## Cierre

`CDX-047` queda terminal como slice transversal de soporte. No publica contenido, no crea snapshot local y no reemplaza la evidencia durable posterior de `CDX-050`; solo deja el adapter minimo para que el foco pueda dejar de depender de checks no resueltos en `main`.
