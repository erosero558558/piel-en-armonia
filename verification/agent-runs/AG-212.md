# AG-212 - Publish deploy observability batch to main and refresh production page

## Summary

- Published the deploy/observability fixes to `main` from a clean worktree, keeping the local dirty workspace isolated.
- Removed the `diagnose-host-connectivity` runtime failure, improved prod verify diagnostics, and fixed the false `critical-css` cache-header failure.
- Re-ran `repair-git-sync` successfully against `main` after the verifier fixes landed.

## Published commits

- `43c1fde0` - `dispatch connectivity diagnosis from deploy-hosting`
- `1d457ed3` - `remove github-script from connectivity diagnosis`
- `22dfe730` - `fix connectivity incident inline script`
- `519f6515` - `fix prod verify asset cache diagnostics`

## Remote validation

- `Diagnose Host Connectivity`
    - `22988991041` succeeded on `22dfe730`
    - `22989166634` succeeded from repair
- `Repair Git Sync (Self-Heal)`
    - `22989573999` succeeded on `519f6515`
    - `Verificar produccion despues de repair` passed
    - `Smoke rapido post-repair` passed
    - fallback dispatch steps were skipped because they were no longer needed

## Live state

- `https://pielarmonia.com/` returns `200`
- page title: `Piel en Armonia | Dermatologia medica en Quito`
- `public_main_sync` still reports:
    - `state=failed`
    - `last_error_message=working_tree_dirty`
    - `telemetry_gap=true`

## Outcome

- The page refresh path is operational again from GitHub Actions and no longer blocked by the false `critical-css` verifier failure.
- The remaining warning is now isolated to the host-side `public_main_sync` cron state, not to the deploy/repair path that this task targeted.

## Proximo paso operativo

- Configurar `SENTRY_AUTH_TOKEN` y `SENTRY_ORG` para cerrar la evidencia Sentry pendiente.
- Re-ejecutar `npm run verify:sentry:events` o el workflow manual `.github/workflows/sentry-events-verify.yml` hasta obtener `status=ok`.
- Revalidar `node bin/prod-readiness-summary.js` y confirmar que `PM-SENTRY-001` deja de ser el pendiente externo principal.
- Triage y rerun remoto de `CI`, `Post-Deploy Gate` y `Deploy Hosting` para sacar `production_stability` y `release_readiness` de `RED`.

## Diagnostico adicional

- `Post-Deploy Gate` remoto quedo sin jobs por un error de compilacion del workflow en GitHub Actions: el commit publicado `877a3d4d` todavia incluia `workflow_dispatch.inputs.skip_figo_post_bench`, llevando el archivo a `26` inputs y superando el limite actual de `25`.
- El worktree local ya corrige ese punto en `.github/workflows/post-deploy-gate.yml`: elimina `skip_figo_post_bench` de `workflow_dispatch`, mueve su control a `vars.SKIP_FIGO_POST_BENCH`, y deja `ASSET_HASH_RETRY_COUNT/DELAY` solo por `vars` para no reabrir inputs ocultos.
- Validacion local del fix operativo:
    - `node --test tests-node/post-deploy-gate-workflow-contract.test.js` -> `6/6`
    - `git diff -- .github/workflows/post-deploy-gate.yml` confirma el recorte de `26 -> 25` inputs respecto al commit remoto fallido.
    - GitHub Actions remoto expone el error textual: `you may only define up to 25 input parameters for a workflow_dispatch event`.
- `bin/prod-readiness-summary.js` ahora tambien clasifica fallos de workflow con diagnostico accionable: distingue `workflow_file_issue` cuando GitHub rechaza el YAML antes de crear jobs, y `staging_canary_missing_secrets` cuando `Deploy Hosting` queda bloqueado por `require_staging_canary=true` sin `STAGING_FTP_SERVER/STAGING_FTP_USERNAME/STAGING_FTP_PASSWORD`.
- El summary real refrescado en `verification/runtime/prod-readiness-summary.{json,md}` ya muestra esa lectura operativa: `Post-Deploy Gate` pasa a `diag: GitHub rechazo el workflow antes de crear jobs`, mientras `Deploy Hosting` queda como bloqueo de configuracion/secrets y no de codigo runtime.
- Hardening adicional de contratos y workspace:
    - `tests-node/prod-readiness-summary-workflows.test.js` cubre parseo BOM del weekly report y la clasificacion de fallos `workflow_file_issue` / `staging_canary_missing_secrets`.
    - `tests-node/workspace-hygiene-contract.test.js` ahora valida superficies root sobre archivos trackeados reales y acepta la invocacion multilinea canonica de `SMOKE-PRODUCCION` desde `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1`.
    - `npm run agent:gate` vuelve a cerrar en verde (`420/420` tests Node, `board legacy check`, `conflicts`, `handoffs lint`, `policy lint`, `codex-check`, `agent validate`).
