# AG-216 - Propagate GitHub deploy alerts into prod ops triage

## Summary

- Wired GitHub `production-alert` issue ingestion into `scripts/ops/prod/REPORTE-SEMANAL-PRODUCCION.ps1` and `scripts/ops/prod/MONITOR-PRODUCCION.ps1`.
- Added `Get-GitHubProductionAlertSummary` and `Invoke-GitHubIssuesGet` to `bin/powershell/Common.Http.ps1`.
- Extended `bin/powershell/Common.Warnings.ps1` so weekly report markdown/JSON now persists `githubDeployAlerts` plus warning codes `github_deploy_*`.
- Updated `scripts/ops/prod/README.md`, `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`, and the Node contracts covering weekly report and prod monitor.

## Validation

- `node --test tests-node/weekly-report-script-contract.test.js tests-node/prod-monitor-public-sync-contract.test.js`
  - Result: `12/12` passing.
- PowerShell parser validation:
  - `scripts/ops/prod/REPORTE-SEMANAL-PRODUCCION.ps1`
  - `scripts/ops/prod/MONITOR-PRODUCCION.ps1`
  - `bin/powershell/Common.Http.ps1`
  - `bin/powershell/Common.Warnings.ps1`
  - Result: no parse errors.
- Live helper validation:
  - `Get-GitHubProductionAlertSummary -Repo erosero558558/piel-en-armonia`
  - Result on 2026-03-12: `fetchOk=true`, `relevantCount=2`, `transportCount=1`, `connectivityCount=1`, issue numbers `#424, #423`.
- Live monitor validation:
  - `pwsh -NoProfile -File scripts/ops/prod/MONITOR-PRODUCCION.ps1 -AllowDegradedPublicSync`
  - Result on 2026-03-12:
    - `health.publicSync` still reports `failureReason=working_tree_dirty` and `telemetryGap=true`.
    - `github.deployAlerts` surfaced the open GitHub deploy incidents:
      - `#424 [connectivity] [ALERTA PROD] Diagnose host connectivity sin ruta de deploy`
      - `#423 [transport] [ALERTA PROD] Deploy Hosting transporte bloqueado desde GitHub Runner`

## Outcome

- Repo-side prod ops triage now shows both host runtime health (`publicSync`) and GitHub-side deploy-route incidents in the same operational lane.
- Remaining production debt is still live infrastructure, not repo instrumentation:
  - `public_main_sync` remains unhealthy with `working_tree_dirty` and `telemetryGap=true`.
  - GitHub-side deploy transport/connectivity alerts remain open while the host route is still blocked.
