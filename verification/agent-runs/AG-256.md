# AG-256

## Alcance

- Release publico unico de `public-v6` con `validated_release_promotion`.
- Nuevo front door paciente con `WhatsApp` como CTA principal canonico.
- Trust layer visible en home y fallback editorial cuando no hay historias aprobadas.
- Alineacion EN/ES de home, hub, servicios, telemedicina y software con la jerarquia nueva.
- Ajuste del contrato de build para aceptar el stage canonico actual de Astro en `.generated/site-root`.
- Correccion menor de copy en software para pasar la auditoria estricta sin lenguaje de runtime confuso.

## Validacion

- `npm run content:public-v6:validate`
- `npm run build:public:v6`
- `npm run audit:public:v6:copy`
- `npm run test:public:v6:copy:contract`
- `node --test tests-node/public-v6-build-contract.test.js tests-node/public-v6-copy-contract.test.js`
- `TEST_LOCAL_SERVER_PORT=8243 TEST_REUSE_EXISTING_SERVER=1 npm run test:frontend:qa:v6`

## Resultado

- El release visible queda listo en un solo corte sobre `AG-256`, sin helper publish separado.
- Home ya no pone mantenimiento de agenda como mensaje dominante; la entrada principal es por WhatsApp.
- `CaseStoriesV6` deja de exponer un empty state protagonista cuando no hay feed aprobado y cede el espacio a `TrustSignalsV6`.
- El contrato de build ahora reconoce el stage canonico actual: `public-v6-shell.js` + assets `/_astro` en `.generated/site-root`, sin exigir `script.js` ni `js/chunks` legacy en staging.
- Toda la suite publica validada en este worktree quedo en verde antes del publish.
- `publish checkpoint AG-256` publico el release en `origin/main` con el commit `6195aad590c7c46b4235946c203e7e7320f89fff`.
- El evento de publish quedo registrado con `release_exception=true`, `live_status=pending`, `verification_pending=true` y `warning_code=publish_live_verification_pending`.
- La verificacion posterior `jobs verify public_main_sync --json` sigue devolviendo `health_missing_public_sync`; el push a `main` ya salio bien, pero la confirmacion host-side queda como deuda operativa separada.
- El seguimiento para restaurar `checks.publicSync` y recuperar la confirmacion live queda abierto en `AG-258`.
