# S12-17 — CTA intelligence por surface

Fecha: 2026-03-30
Owner: Codex-erosero558558

## Objetivo

Hacer que cada surface publica empuje al siguiente paso correcto:

- Servicio -> booking de esa especialidad
- Primera consulta -> WhatsApp con texto prepopulado
- Blog -> pre-consulta

## Publicacion verificada

Se regenero la salida publica V6 con:

```bash
npm run build:public:v6
```

La salida publicada quedo sincronizada en `.generated/site-root/`.

## Verificaciones

Se confirmo sobre el stage local servido por `bin/local-stage-router.php` en `http://127.0.0.1:8013`:

```bash
TEST_BASE_URL=http://127.0.0.1:8013 npx playwright test tests/service-booking-hints-parity.spec.js tests/public-blog-index.spec.js tests/public-blog-article.spec.js tests/public-first-consultation.spec.js --workers=1
```

Resultado:

- `11 passed`

Checks manuales de HTML generado:

- `.generated/site-root/es/servicios/acne-rosacea/index.html` -> CTA `/es/?service=acne#citas`
- `.generated/site-root/en/services/botox/index.html` -> CTA `/en/?service=rejuvenecimiento#citas`
- `.generated/site-root/es/blog/index.html` -> CTA `/es/pre-consulta/`
- `.generated/site-root/es/blog/como-elegir-dermatologo-quito/index.html` -> CTA `/es/pre-consulta/`
- `.generated/site-root/es/primera-consulta/index.html` -> CTA WhatsApp con texto prepopulado

## Nota operativa

Durante la validacion aparecio un servidor viejo en `127.0.0.1:8011` que respondia `404` para algunas rutas V6. La validacion final se hizo contra un stage limpio en `127.0.0.1:8013`.
