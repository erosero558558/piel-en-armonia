# AG-245 — Reconstructed Evidence

Reconstructed on 2026-03-21 because the canonical evidence file was missing.

Task snapshot:
- Title: `Pilot readiness evidence: explain openclaw runtime red for admin operativo`
- Files: `tools/agent-orchestrator/domain/runtime.js`, `tools/agent-orchestrator/commands/runtime.js`, `TRI_LANE_RUNTIME_RUNBOOK.md`

Current reproducible checks:
- `node --test tests-node/agent-orchestrator-cli.test.js` passed on 2026-03-21.
- `node agent-orchestrator.js runtime verify openclaw_chatgpt --json` currently returns `ok=false` with:
  - `figo_queue=unhealthy(bridge_unhealthy)`
  - `leadops_worker=unhealthy(worker_disabled)`
  - `operator_auth=unhealthy(auth_status_http_502)`

Notes:
- This evidence is intentionally current-state oriented: it records the live runtime failure mode that the task family was meant to explain.
