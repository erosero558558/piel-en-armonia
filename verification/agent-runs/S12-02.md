# S12-02

Fecha: 2026-03-30

## Resumen

Se cerró la auditoría de calidad de landings de servicios pedida en `S12-02`. El entregable quedó en `docs/SERVICE_LANDING_QUALITY_AUDIT.md` con score por página y top 5 de rediseño prioritario.

## Criterios auditados

- CTA claro
- Before/after
- Precio referencial
- Testimonio
- FAQ

## Resultado global

- `19/19` con CTA claro
- `19/19` con FAQ
- `0/19` con before/after
- `0/19` con precio referencial
- `0/19` con testimonio

Todos los servicios quedaron en `40/100` dentro del rubric editorial. La priorización final se rompió por intención comercial, riesgo percibido y desalineación del mensaje.

## Top 5 priorizado

1. `laser-dermatologico`
2. `depilacion-laser`
3. `rellenos-hialuronico`
4. `microdermoabrasion`
5. `botox`

## Comandos usados

```bash
find es/servicios -mindepth 2 -maxdepth 2 -name 'index.html' | sort
node bin/claim.js claim S12-02 "Codex-erosero558558"
rg -n "S12-02|Service landing quality audit" AGENTS.md
node - <<'NODE'
const fs=require('fs');
const path=require('path');
for (const dir of fs.readdirSync('es/servicios').sort()) {
  const file=path.join('es/servicios',dir,'index.html');
  if (!fs.existsSync(file)) continue;
  const html=fs.readFileSync(file,'utf8');
  const template=/data-public-shell-version="v6"|v6-booking-status/.test(html)?'v6':'legacy';
  const cta=/service-mobile-cta|v6-booking-status|https:\/\/wa\.me\//i.test(html);
  const faq=/Dudas frecuentes|Preguntas frecuentes|service-faq|v6-service-faq/i.test(html);
  console.log(JSON.stringify({dir,template,cta,faq}));
}
NODE
```

## Validación pendiente de cierre

Pendiente correr `node bin/gate.js S12-02` y liberar el claim como parte del cierre final.
