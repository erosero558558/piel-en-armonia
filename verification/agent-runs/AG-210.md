# AG-210 - Add structured connectivity incident to diagnose-host-connectivity

## Summary

- Extended `diagnose-host-connectivity.yml` so the workflow now publishes a structured connectivity artifact alongside the existing text report.
- Added a dedicated production incident path for the case where all configured host origins end without any reachable deploy target.
- Updated the public main update runbook to document the new artifact set and incident lifecycle.

## Files

- `.github/workflows/diagnose-host-connectivity.yml`
- `tests-node/deploy-hosting-workflow-contract.test.js`
- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`

## Verification

- `node --test tests-node/deploy-hosting-workflow-contract.test.js`

## Result

- Contract suite passed: `10/10`.
- `diagnose-host-connectivity.yml` now uploads both `connectivity-report.txt` and `connectivity-report.json`, plus a workflow summary with connectivity status, configured hosts and open targets.
- The workflow now creates or updates `[ALERTA PROD] Diagnose host connectivity sin ruta de deploy` when no configured origin exposes any open deploy path, and closes that incident automatically once a later diagnose run observes at least one reachable target.
