# AG-210 - Public cutover hidden artifact upload fix

## Summary

- Enabled `include-hidden-files: true` for the `public-cutover-evidence` upload in `deploy-hosting.yml`.
- Extended the workflow contract so early transport failures keep asserting that `.public-cutover/transport-preflight.json` is uploadable as evidence, not only generated locally.

## Verification

- `node --test tests-node/deploy-hosting-workflow-contract.test.js`

## Expected runtime outcome

- `Upload public cutover evidence` should stop warning about missing files when `.public-cutover/transport-preflight.json` exists.
