# CDX-060 - Admin queue pilot UI specs and fixtures

## Resumen

- Se reemplazo `tests/admin-queue.spec.js` por cinco specs focalizadas:
    - `tests/admin-queue-guidance-live-ops.spec.js`
    - `tests/admin-queue-reception-escalation.spec.js`
    - `tests/admin-queue-controls-numpad.spec.js`
    - `tests/admin-queue-pilot-governance.spec.js`
    - `tests/admin-queue-ops-hub.spec.js`
- Se agrego `tests/helpers/admin-queue-fixtures.js` para compartir helpers del piloto `admin-queue`.
- La adaptacion al trim visual del piloto abre explicitamente los grupos `details` de validacion y consolas avanzadas cuando el spec necesita inspeccionarlos.
- Los mount hosts del ops hub que hoy funcionan como wrappers se validan con `toBeAttached()` mientras que los componentes montados visibles siguen validandose con `toBeVisible()`.
- El runtime local del clone actualizado requirio una regeneracion previa con `npm run build:turnero:runtime` porque `admin.js` apuntaba a `js/admin-chunks/index-BFFMNQf-.js` y el chunk no estaba presente tras checkout limpio.

## Comandos ejecutados

```bash
npm run build:turnero:runtime
npm run check:turnero:runtime
TEST_LOCAL_SERVER_ENGINE=node TEST_LOCAL_SERVER_PORT=9174 node bin/run-playwright-local.js tests/admin-queue-guidance-live-ops.spec.js tests/admin-queue-reception-escalation.spec.js tests/admin-queue-controls-numpad.spec.js tests/admin-queue-pilot-governance.spec.js tests/admin-queue-ops-hub.spec.js tests/queue-integrated-flow.spec.js --workers=1
TEST_LOCAL_SERVER_ENGINE=node TEST_LOCAL_SERVER_PORT=9168 node bin/run-playwright-local.js tests/turnero-runtime-smoke.spec.js --workers=1
TEST_LOCAL_SERVER_ENGINE=node TEST_LOCAL_SERVER_PORT=9176 node bin/run-playwright-local.js tests/queue-display.spec.js tests/admin-queue-guidance-live-ops.spec.js tests/admin-queue-reception-escalation.spec.js tests/admin-queue-controls-numpad.spec.js tests/admin-queue-pilot-governance.spec.js tests/admin-queue-ops-hub.spec.js tests/queue-kiosk.spec.js tests/queue-operator.spec.js tests/queue-integrated-flow.spec.js tests/turnero-sony-premium-contract.spec.js tests/turnero-surface-rollout-wiring.spec.js --workers=1
```

## Resultado

- Build/runtime local: verde.
- `test:admin:queue` materializado con el wrapper canónico local: `96 passed (12.6m)`.
- `test:turnero:ui` materializado con el wrapper canónico local:
    - `tests/turnero-runtime-smoke.spec.js`: `4 skipped`, exit `0`.
    - bloque principal de UI turnero: `157 passed (15.0m)`.
- Snapshot local del foco escrito en `verification/focus-checks/FOCUS-2026-03-admin-operativo-cut-1.json`.
- El symlink temporal local `node_modules -> /home/deck/Documents/GitHub/piel-en-armonia/node_modules` se uso solo para correr Playwright en el worktree de rescate y no forma parte del publish scope.
- La primera corrida cruda con `npx playwright test` sobre el servidor local por defecto produjo falsos rojos en `turnero-runtime-smoke.spec.js` y `admin-queue-ops-hub.spec.js`; el corte válido para gobernanza es el del wrapper `bin/run-playwright-local.js` con `TEST_LOCAL_SERVER_ENGINE=node`, que es la vía canónica del repo para estos checks locales.
- La metadata final de la slice queda en `backend_ops/gates` por politica canonica del repo: `tests/**` y `tests/helpers/**` se clasifican como `backend_ops`, aunque el contenido siga siendo smoke UI del piloto `admin-queue`.
