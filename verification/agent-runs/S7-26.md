# S7-26

## Resumen

Se creó `docs/OWNERSHIP.md` para dejar explícito quién responde por las zonas
críticas del repo y qué handoff mínimo debe existir antes de delegarlas.

## Cambios

- Se agregó `docs/OWNERSHIP.md` con tabla de ownership por zona crítica.
- La tabla cubre al menos `lib/auth.php`, `lib/clinical_history/`,
  `controllers/OpenclawController.php`, `k8s/` y `ops/caddy/`.
- Se añadieron zonas operativas vecinas de alto riesgo para reducir el bus
  factor del release path y desktop distribution.
- Se enlazó el documento desde `docs/OPERATIONS_INDEX.md`.

## Validacion ejecutada

```bash
rg -n "OWNERSHIP|lib/auth.php|lib/clinical_history/|controllers/OpenclawController.php|k8s/|ops/caddy/" docs/OWNERSHIP.md docs/OPERATIONS_INDEX.md
```

Resultados:

- `docs/OWNERSHIP.md` existe y contiene las superficies mínimas pedidas.
- `docs/OPERATIONS_INDEX.md` ya apunta al documento para descubribilidad.

## Notas

- Durante el cierre aparecieron cambios ajenos en `tests/tmp-admin-debug.spec.js`
  y claims `UI-*`; se dejaron fuera del scope de `S7-26`.
