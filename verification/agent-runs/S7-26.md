# S7-26 - Docs de ownership por zona

- Date: `2026-03-29`
- Task title: `Docs de ownership por zona`

## Outcome

- Added `docs/OWNERSHIP.md` as the canonical map of human ownership for the
  repo's critical zones.
- Covered the five minimum surfaces requested by the task:
  `lib/auth.php`, `lib/clinical_history/`,
  `controllers/OpenclawController.php`, `k8s/` and `ops/caddy/`.
- Documented the minimum handoff packet required before publishing changes in
  each zone (`AUTH-1`, `HCE-1`, `OPENCLAW-1`, `K8S-1`, `CADDY-1`).
- Captured the current documentation gap: the repo references a "contacto
  tecnico designado" but does not yet publish a single named infra owner.
- Marked `S7-26` as done in `AGENTS.md`.

## Verification

- `git diff --check` -> `PASS`
- `node bin/gate.js S7-26` -> `PASS`

## Notes

- This document is about human operational ownership, not Codex lane
  ownership. Technical lane ownership continues to live in `AGENTS.md`.
