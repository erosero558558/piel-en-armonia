# S15-03 — Dispatch role preferences for S8-S14

## Summary

- Updated `ROLE_AFFINITY.prefer[]` in `bin/dispatch.js` so backend, frontend, and devops prioritize S8-S14 work before older generic backlog.
- Wrapped the dispatch CLI in `main()` and exposed helper exports so the role-affinity table can be tested directly.
- Added `tests-node/dispatch-cli.test.js` to lock the required preference lists and selection behavior by role.

## Validation

- `node --check bin/dispatch.js`
- `node --test tests-node/dispatch-cli.test.js`
- `npm run dispatch:backend`
- `npm run dispatch:frontend`
- `node bin/dispatch.js --role devops`
- `git diff --check`

## Result

- Backend now recommends `S9-08`.
- Frontend now recommends `S9-01`.
- Devops now recommends `S14-02`.
