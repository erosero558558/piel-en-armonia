# UI2-20 - Extender verify.js con checks de Fase 2

Fecha: `2026-03-30`

## Objetivo

Agregar verificaciones automaticas en [bin/verify.js](/Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm/bin/verify.js) para los gaps criticos detectados en la auditoria UI de Fase 2 y dejar cobertura en tests.

## Cambios

- Se amplio `TASK_LINE_PATTERN` para reconocer IDs `UI2-*` ademas de los sprints clasicos.
- Se agregaron helpers para medir cobertura de `aurora-service.css`, detectar `aria-live`, `prefers-reduced-motion`, `shortcuts` en `manifest.json` y `fetch(` en `es/portal/index.html`.
- Se agrego la regla `UI2-20` al verificador para que el board pueda detectar que la extension ya existe.
- Se cubrio el comportamiento en [tests-node/verify-cli.test.js](/Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm/tests-node/verify-cli.test.js).

## Estado actual de los checks

- `es/servicios/*/index.html`: `19` paginas detectadas, `1` importa `aurora-service.css`, `18` siguen faltando.
- `sala-turnos.html`: `aria-live` presente.
- `styles/base.css`: `prefers-reduced-motion` aun no presente.
- `manifest.json`: `shortcuts` aun no presente.
- `es/portal/index.html`: `fetch(` aun no presente.

## Verificacion

```bash
node --check bin/verify.js
node --check tests-node/verify-cli.test.js
node --test tests-node/verify-cli.test.js
node bin/verify.js
```

## Nota operativa

`node bin/gate.js UI2-20` todavia no es compatible con IDs `UI2-*`; el script solo acepta tareas `S*`. Para este cierre use verificacion manual mas la suite focalizada de `verify.js`.
