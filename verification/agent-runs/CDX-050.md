# CDX-050 - Public V6 ES publish readiness evidence snapshot

Fecha: 2026-03-28
Owner: Ernesto
Executor: codex
Lane: `codex_transversal`
Subfrente: `SF-transversal-public-v6-es-support`

## Objetivo

Cerrar `publish_readiness_review` del foco `Public V6 ES` sin tocar copy, runtime publico ni `public_main_sync`, dejando:

- snapshot local canonico de required checks para `FOCUS-2026-03-public-v6-es-voz-cut-1`
- evidencia durable de readiness en este archivo
- workspace sin forks activos de board/worktree asociados al intento historico `CDX-049`

## Contrato aplicado

- `focus verify` materializa `verification/focus-checks/<focus_id>.json` como cache local del foco.
- El snapshot no ejecuta checks; canoniza el estado ya resuelto por evidencia de `CDX-045` y `CDX-048`.
- Si existe un snapshot local stale, `focus verify` lo regenera desde esa evidencia canonica en lugar de perpetuar el estado viejo.
- `focus status` y `focus check` prefieren el snapshot JSON cuando es valido; si no existe, bootstrapean desde evidencia markdown de slices `review|done`.
- Los aliases locales se normalizan para que `audit:public:v6:copy` y `audit:public-v6:copy` cuenten como el mismo required check.
- `focus_status` cierra en `closed` al terminar la slice; el readiness durable queda expresado via `focus_evidence_ref` y `CDX-050`.

## Limpieza y gobernanza

- Se agregaron `.codex-local/`, `.codex-worktrees/` y `verification/focus-checks/` al ignore canonico como artefactos locales.
- El worktree `/Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm-main-origin-integration-20260327` ya no participa del set activo de `git worktree`; la referencia historica queda preservada en la rama local `codex/integrate-main-cdx049`.
- `CDX-049` no se reabre: queda absorbido por `CDX-047` y solo se limpia su residuo operativo.
- `CDX-048` conserva `review` como slice frontend vigente; `CDX-050` cierra solo la capa transversal de readiness.

## Validacion

Ejecutado con `node v20` local:

```bash
node --test --test-name-pattern "loadLocalRequiredCheckSnapshot|evaluateRequiredChecks resuelve content/audit/test desde snapshot local valido" tests-node/orchestrator/focus.test.js
node --test --test-name-pattern "focus verify materializa snapshot JSON canonico desde evidencia local ya validada|focus status expone foco activo y checks requeridos|focus check solo endurece required checks con --enforce-required-checks" tests-node/agent-orchestrator-cli.test.js
node agent-orchestrator.js focus verify --json
node agent-orchestrator.js focus status --json
node agent-orchestrator.js focus check --enforce-required-checks --json
node agent-orchestrator.js status --json
node agent-orchestrator.js codex-check --json
node agent-orchestrator.js board doctor --json --profile ci
node agent-orchestrator.js handoffs lint --json
git worktree list --porcelain
```

Resultado esperado:

- snapshot local valido en `verification/focus-checks/FOCUS-2026-03-public-v6-es-voz-cut-1.json`
- `required_checks_ok=true`
- desaparece `required_check_unverified`
- desaparece `board_fork`
- `workspace_truth.ok=true`
- `public_main_sync` permanece solo como warning externo tolerado
