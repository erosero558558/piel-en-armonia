# AG-258 — Blocked Rollout Evidence

Recorded on 2026-03-21 while remediating governance debt and host/runtime health.

Task snapshot:

- Title: `Rollout publico de health/publicSync y verificacion host-side`
- Scope: deploy / backend ops / critical zone
- Files: `controllers/HealthController.php`, `.github/workflows/deploy-hosting.yml`, `.github/workflows/deploy-frontend-selfhosted.yml`, `.github/workflows/post-deploy-fast.yml`, `.github/workflows/post-deploy-gate.yml`, `.github/workflows/prod-monitor.yml`, `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`

Board actions executed during this remediation:

- `node agent-orchestrator.js task start AG-258 --status blocked --blocked-reason host_side_runtime_mismatch_and_remote_verification_pending --expect-rev 735 --json`
- `node agent-orchestrator.js leases clear AG-258 --reason blocked_remote_verification_pending --expect-rev 736 --json`
- `AG-258` remains `blocked` and is now recognized by focus/status as an acknowledged external blocker.

Current host-side verification:

- `node agent-orchestrator.js jobs verify public_main_sync --json` -> `ok=false`, `verification_source=health_url`, `failure_reason=stale`, `last_success_at=2026-03-19T05:51:08.9500800Z`
- `node agent-orchestrator.js runtime verify openclaw_chatgpt --json` -> `ok=false`
    - `figo_queue=degraded(legacy_proxy_without_gateway)`
    - `leadops_worker=unhealthy(worker_disabled)`
    - `operator_auth=unhealthy(operator_auth_not_configured)`
- Direct probes on 2026-03-21:
    - `GET /api.php?resource=health` -> `200` with `checks.publicSync.healthy=false` and `failureReason=stale`
    - `GET /api.php?resource=operator-auth-status` -> `200` with `status=operator_auth_not_configured`
    - `GET /figo-ai-bridge.php` -> `200` with `providerMode=legacy_proxy` and `gatewayConfigured=false`

Conclusion:

- The rollout is still not safely closable from this repo alone.
- The repo-side contracts and governance checks were repaired, but the live host still needs host-side remediation for stale `public_main_sync`, incomplete `operator_auth` broker config, and OpenClaw bridge/worker readiness.
