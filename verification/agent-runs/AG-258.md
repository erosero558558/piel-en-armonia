# AG-258 — Blocked Rollout Evidence

Recorded on 2026-03-21 while remediating governance debt and host/runtime health.

Task snapshot:
- Title: `Rollout publico de health/publicSync y verificacion host-side`
- Scope: deploy / backend ops / critical zone
- Files: `controllers/HealthController.php`, `.github/workflows/deploy-hosting.yml`, `.github/workflows/deploy-frontend-selfhosted.yml`, `.github/workflows/post-deploy-fast.yml`, `.github/workflows/post-deploy-gate.yml`, `.github/workflows/prod-monitor.yml`, `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`

Board actions executed during this remediation:
- `node agent-orchestrator.js task start AG-258 --status blocked --blocked-reason host_502_publicsync_operator_auth_and_no_host_access --expect-rev 732 --json`
- `node agent-orchestrator.js leases clear AG-258 --reason blocked_host_502_and_no_host_access --expect-rev 733 --json`
- Current board state still shows a tracked, non-required lease for the blocked task; it is not expired and is not the active blocker.

Current host-side verification:
- `node agent-orchestrator.js jobs verify public_main_sync --json` -> `ok=false`, `verification_source=registry_only`, `failure_reason=unverified`
- `node agent-orchestrator.js runtime verify openclaw_chatgpt --json` -> `ok=false`
  - `figo_queue=unhealthy(bridge_unhealthy)`
  - `leadops_worker=unhealthy(worker_disabled)`
  - `operator_auth=unhealthy(auth_status_http_502)`
- Direct probes on 2026-03-21:
  - `GET /api.php?resource=health` -> HTTP 502
  - `GET /api.php?resource=operator-auth-status` -> HTTP 502
  - `GET /admin-auth.php?action=status` -> HTTP 502
  - `GET /figo-ai-bridge.php` -> HTTP 502

Conclusion:
- The rollout is not safely closable from this repo alone.
- The task was moved out of `in_progress` and into `blocked` so the board no longer claims active healthy progress while the live host remains inaccessible.
