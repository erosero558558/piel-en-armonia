# CDX-062

## Resumen

- Slice `backend_ops` de soporte del frente
  `STRAT-2026-03-turnero-web-pilot-multi-clinic-local`.
- Su alcance es validar y stagear `clinica-norte-demo` como segunda clínica
  `web_pilot` sin cambiar el schema `turnero-clinic-profile/v1` ni reemplazar
  el `clinic-profile.json` activo.
- Expone el catálogo multi-clínica hacia PHP/admin-data para que el admin vea
  la segunda clínica como local-ready.

## Write Set

- `controllers/AdminDataController.php`
- `lib/TurneroClinicProfile.php`
- `bin/run-turnero-web-pilot-ui.js`
- `package.json`
- `tests-node/admin-data-turnero-clinic-profile-contract.test.js`
- `tests-node/turnero-clinic-profile-registry.test.js`
- `tests-node/turnero-clinic-profile-cli.test.js`
- `verification/agent-runs/CDX-062.md`

## Cambios aplicados

- `TurneroClinicProfile` ahora expone el catálogo completo y el resumen
  regional de clínicas `web_pilot`.
- `AdminDataController` incorpora `turneroClinicProfiles` y
  `turneroRegionalClinics` dentro del payload consumido por admin y queue
  operator.
- La CLI y el contrato de catálogo aceptan a `clinica-norte-demo` como
  `web_pilot` local-first con nativas no bloqueantes.
- El gate local `turnero:web-pilot` dejó de apuntar al spec eliminado
  `tests/admin-queue.spec.js` y ahora usa
  `tests/admin-queue-pilot-governance.spec.js` como suite canónica del carril
  Admin queue.
- El perfil activo versionado no cambia: el stage por `--id` sigue siendo
  explícito y reproducible por clínica.

## Validación

```bash
node bin/turnero-clinic-profile.js validate --id clinica-norte-demo --json
node bin/turnero-clinic-profile.js stage --id clinica-norte-demo --dry-run --json
node --test tests-node/turnero-clinic-profile-registry.test.js tests-node/turnero-clinic-profile-cli.test.js tests-node/admin-data-turnero-clinic-profile-contract.test.js tests-node/turnero-runtime-contract.test.js
npm run test:turnero:web-pilot:contracts
npm run test:turnero:web-pilot:php-contract
npm run gate:turnero:web-pilot
```

Resultado:

- `validate --id clinica-norte-demo --json`: OK.
- `stage --id clinica-norte-demo --dry-run --json`: OK.
- Targeted Node contracts verdes (`20/20`).
- `test:turnero:web-pilot:contracts`: verde (`186/186`).
- `test:turnero:web-pilot:php-contract`: verde.
- `gate:turnero:web-pilot`: verde después de corregir el wiring del runner
  `Admin queue`.

## Required Checks

- required_check: test:turnero:web-pilot:contracts | state: green | command: npm run test:turnero:web-pilot:contracts
- required_check: test:turnero:web-pilot:php-contract | state: green | command: npm run test:turnero:web-pilot:php-contract
- required_check: test:turnero:web-pilot:ui | state: green | command: npm run test:turnero:web-pilot:ui

## Notas

- `clinica-norte-demo` queda catalogada y stageable como segunda clínica
  `web_pilot`, pero el repo conserva un único perfil activo versionado.
- El ajuste en `package.json` y `bin/run-turnero-web-pilot-ui.js` es de
  soporte/gating; no cambia el comportamiento funcional del producto.
- Este bloque no toca `verify-remote`, deploy productivo ni recuperación de
  `public_main_sync`.
- Esta evidencia queda como cierre canónico de `CDX-062`; el closeout del
  foco y de la estrategia se concentra después en `CDX-063`.
