# AG-195 - Public main sync canonical deploy stabilization

- Date: `2026-03-11`
- Task title: `Public main sync canonical deploy stabilization`

## Outcome

- `bin/deploy-public-v3-live.sh` now promotes the versioned public artifacts already committed in `main` instead of rebuilding Astro on the VPS during `public_main_sync`.
- `bin/deploy-public-v3-cron-sync.sh` now attempts a conservative self-heal for canonical generated drift (`es/**`, `en/**`, `_astro/**`, runtime assets, and Composer metadata) before failing with `working_tree_dirty`.
- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md` and `docs/DEPLOYMENT.md` now describe the canonical publish model consistently: local build first, committed artifacts as source of truth, host sync as promotion only.

## Verification

- `node --test tests-node/deploy-public-v3-live-script.test.js tests-node/deploy-public-v3-cron-sync-script.test.js` -> `PASS` (`11/11`)
- `node --test tests-node/agent-jobs-cli.test.js tests-node/publish-checkpoint-command.test.js tests-node/orchestrator/jobs-engine.test.js` -> `PASS` (`8/8`)
- `node agent-orchestrator.js board doctor --json` -> `OK`
- `node agent-orchestrator.js conflicts --json` -> `0` blocking conflicts

## Operational Note

- `node agent-orchestrator.js jobs verify public_main_sync --json` still reports `healthy=false` on `2026-03-11` with `last_error_message=working_tree_dirty`.
- That live warning reflects the currently deployed host state, not the local code after this task.
- A publish of this repo state to `main` plus the next successful cron run is still required to validate the fix in production.
