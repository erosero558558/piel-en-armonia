## AG-221 - Alinear specs OpenClaw compartidos con el shell de login nuevo

### Resumen

- Se releyo el WIP actual de `admin-v3` y `queue-operator` para confirmar que el shell nuevo de login OpenClaw ya esta presente en este worktree.
- No hizo falta editar codigo en este bloque: los specs `operator-auth-shared-session` y `operator-auth-local-real` ya estaban alineados al shell `#loginForm + #openclawLoginStage + #legacyLoginStage`.

### Verificacion

- `npx playwright test tests/operator-auth-shared-session.spec.js --workers=1`
    - Resultado: `2/2` en verde.
- `tests/operator-auth-local-real.spec.js`
    - Verificado por lectura: ya usa el shell nuevo y queda condicionado por `TEST_REAL_OPERATOR_AUTH`.

### Resultado

- Se confirma que el drift funcional esperado para `operator-auth-shared-session` ya estaba resuelto dentro del WIP local.
- El siguiente frente real queda identificado en la deuda de mocks inline de `admin-auth.php` en `queue-operator.spec.js` y otros specs admin/operator.
