# AG-217 - Propagate GitHub deploy alerts into deploy verify and smoke gates

## Summary

- Extended `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1` to resolve live GitHub `production-alert` issues, fail on open deploy incidents by default, persist `github-deploy-*` assets into `verification/last-deploy-verify.json`, and forward the same settings to integrated smoke runs.
- Extended `scripts/ops/prod/SMOKE-PRODUCCION.ps1` to resolve the same GitHub deploy alerts under `-RequireCronReady`, fail by default when those incidents remain open, and support observation mode through `-AllowOpenGitHubDeployAlerts`.
- Updated `scripts/ops/prod/README.md`, `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`, and the Node contracts in `tests-node/prod-ops-public-sync-contract.test.js` plus `tests-node/prod-verify-asset-cache-contract.test.js`.

## Validation

### Static contracts

```text
node --test tests-node/prod-ops-public-sync-contract.test.js tests-node/prod-verify-asset-cache-contract.test.js
```

- Result: `8/8` tests passing.

### PowerShell parser

```text
pwsh -NoProfile -Command '& { $files = @(''scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1'', ''scripts/ops/prod/SMOKE-PRODUCCION.ps1''); foreach ($file in $files) { $tokens = $null; $errors = $null; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $file), [ref]$tokens, [ref]$errors) | Out-Null; if ($errors -and $errors.Count -gt 0) { Write-Output (''PARSE_ERROR '' + $file); $errors | ForEach-Object { Write-Output $_.Message }; exit 1 }; Write-Output (''PARSE_OK '' + $file) } }'
```

- Result:
  - `PARSE_OK scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1`
  - `PARSE_OK scripts/ops/prod/SMOKE-PRODUCCION.ps1`

### Live verify

```text
pwsh -NoProfile -File scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1 -RequireCronReady -AllowDegradedFigo -AllowRecursiveFigo -AllowMetaCspFallback -SkipAssetHashChecks
```

- Result highlights:
  - `public sync lastErrorMessage=working_tree_dirty ... telemetryGap=True`
  - `github.deployAlerts fetchOk=True repo=erosero558558/piel-en-armonia relevantCount=2 transportCount=1 connectivityCount=1`
  - `github.deployAlerts open production alerts (count=2 issueNumbers=#424, #423)`
  - `github.deployAlerts transport blocked (issueNumbers=#424, #423)`
  - `github.deployAlerts deploy connectivity blocked (issueNumbers=#424, #423)`
  - final status: `Resultado: 6 falla(s) detectadas.`

### Live verify observation mode

```text
pwsh -NoProfile -File scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1 -RequireCronReady -AllowDegradedFigo -AllowRecursiveFigo -AllowMetaCspFallback -SkipAssetHashChecks -AllowOpenGitHubDeployAlerts
```

- Result highlights:
  - `github.deployAlerts ... relevantCount=2 transportCount=1 connectivityCount=1`
  - `WARN github.deployAlerts open production alerts ...`
  - `WARN github.deployAlerts transport blocked ...`
  - `WARN github.deployAlerts deploy connectivity blocked ...`
  - final status still fails for host-side `publicSync`: `Resultado: 3 falla(s) detectadas.`

### Live smoke

```text
pwsh -NoProfile -File scripts/ops/prod/SMOKE-PRODUCCION.ps1 -RequireCronReady -AllowDegradedFigo -AllowRecursiveFigo -AllowMetaCspFallback
```

- Result highlights:
  - `checks.publicSync.healthy=false (state=failed, lastErrorMessage=working_tree_dirty, dirtyPathsCount=0)`
  - `checks.publicSync failed sin telemetria runtime suficiente`
  - `github.deployAlerts fetchOk=True repo=erosero558558/piel-en-armonia relevantCount=2 transportCount=1 connectivityCount=1`
  - `github.deployAlerts open production alerts (count=2 issueNumbers=#424, #423)`
  - `github.deployAlerts transport blocked (issueNumbers=#424, #423)`
  - `github.deployAlerts deploy connectivity blocked (issueNumbers=#424, #423)`
  - final status: `Fallas de contrato/reglas: 3`

## Runtime status

- Live GitHub deploy incidents corroborated on 2026-03-12:
  - `#423 [transport] [ALERTA PROD] Deploy Hosting transporte bloqueado desde GitHub Runner`
  - `#424 [connectivity] [ALERTA PROD] Diagnose host connectivity sin ruta de deploy`
- `public_main_sync` remains unhealthy from `health_url` with `failure_reason=working_tree_dirty` and `telemetry_gap=true`.
