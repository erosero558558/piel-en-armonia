# AG-059 - Service pages conversion UX + QA hardening (acne/laser)

## Estado

- `done`

## Objetivo del bloque

- Mejorar conversion y escaneabilidad en paginas de servicios (`acne` y `laser`) sin tocar backend.
- Agregar QA Playwright especifico para estas superficies (desktop/mobile + tema + CTA/quick-dock).
- Integrar el QA de servicios al cierre QA publico para regresion continua.

## Cambios del bloque

- Nuevos paneles de intencion/conversion en hero de `servicios/acne.html` y `servicios/laser.html`:
  - badge contextual
  - 3 tarjetas de decision rapida
  - CTA a `#citas`
  - CTA WhatsApp con copy especifico por servicio
- Estilos nuevos en `styles.css` para `.service-intent-*` (desktop/mobile + dark mode).
- `scroll-margin-top` para `#citas` en paginas de servicio (`.service-page #citas`).
- Nuevo spec Playwright `tests/service-pages-qa.spec.js` cubriendo:
  - cues de conversion en desktop
  - persistencia de tema oscuro
  - layout mobile sin overflow del panel/quick-dock
  - avance util hacia `#citas` y disponibilidad del quick-dock en mobile
- Script nuevo `npm run test:frontend:qa:services`.
- `test:frontend:qa:public` actualizado para incluir el QA de servicios.

## Validacion local (2026-02-26)

- `npm run build` -> `success`
- `npm run test:frontend:qa:services` -> `success` (`4 passed`)
- `npm run test:frontend:qa:public` -> `success` (`42 passed`)
- `node agent-orchestrator.js board doctor --json` -> `ok: true` (warn-first esperado)

## Criterio de aceptacion cubierto

- Paginas de servicios `acne/laser` muestran mejoras visibles de conversion y mantienen QA publico repetible sin regresiones de layout/tema en mobile/desktop.
