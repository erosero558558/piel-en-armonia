# S13-15

## Resumen

- Se amplió `bin/verify.js` de `52` a `100` reglas verificables y se formalizó un registro tipado de evidencia con `file_exists`, `grep` y `json_key`.
- El runtime ahora distingue explícitamente entre `doneWithoutEvidence` y `doneWithoutRule`, para que las tareas marcadas `[x]` sin prueba verificable ya no queden ocultas.
- También se corrigieron checks legacy (`S2-18`, `S2-24`) para dejar de depender de `find` contra rutas ausentes y se normalizó el exit code del CLI a `0|1`.
- Se añadió gate específico para `S13-15` en `bin/lib/gate-checks.js` y cobertura en `tests-node/gate-task-checks.test.js` y `tests-node/verify-cli.test.js`.

## Verificacion

- `node --test tests-node/verify-cli.test.js`
- `node --test --test-name-pattern="S13-15" tests-node/gate-task-checks.test.js`
- `node bin/gate.js S13-15`
- `node bin/verify.js`

## Resultado

- `verify.js` ahora reporta deuda visible en vez de esconderla: `79/100 verified done`, `21 done-without-evidence` y `254 done-without-rule`.
- El gate de `S13-15` pasa y protege el contrato de cobertura mínima (`100+` reglas) más evidencia tipada por tarea.
- El comando deja de devolver un conteo saturado como exit code y falla limpiamente con `1` cuando hay discrepancias reales.
