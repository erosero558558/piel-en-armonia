# AG-255

## Alcance

- Reorden de la portada de `admin.html` para OpenClaw con foco operativo.
- Separacion visible entre via activa, estado del acceso y siguiente paso.
- Contingencia `clave + 2FA` solo cuando el backend la habilita.
- Ajuste de copy y estados para `local_helper`, `web_broker` y contingencia.

## Validacion local

- `npx playwright test tests/admin-openclaw-login.spec.js`
- `npm run test:admin:openclaw-auth`
- `npm run test:frontend:qa:admin`
- `npm run build`

## Resultado

- Todas las validaciones locales quedaron en verde.
- El build regenero `admin.html`, `admin.js` y el chunk canonico de admin.

## Gate remoto

- `npm run gate:admin:rollout:openclaw` no pudo ejecutarse en este entorno porque `powershell` no esta instalado.
- Se ejecuto la variante equivalente `npm run gate:admin:rollout:openclaw:node`.
- El gate remoto confirmo `admin.html`, CSP y contrato OpenClaw publicados.
- El gate remoto fallo en los smokes de `admin-v3-runtime` y `admin-openclaw-auth` sobre `https://pielarmonia.com` porque el dominio publico todavia sirve la version anterior del login y aun no refleja este cambio local.

## Seguimiento

- Se agregaron estados terminales visibles para `identity_unverified` y `broker_claims_invalid` en admin y turnero.
- El flujo OpenClaw ahora conserva `csrfToken` entre `status` y `start` para evitar intentos anonimos rotos en superficies compartidas.
- Se ajusto el helper de pruebas para iniciar OpenClaw con `X-CSRF-Token` cuando el backend lo exige.

## Validacion adicional

- `TEST_REUSE_EXISTING_SERVER=1 TEST_LOCAL_SERVER_PORT=8230 npx playwright test tests/admin-openclaw-login.spec.js tests/operator-auth-shared-session.spec.js --workers=1 --reporter=line`
- Resultado: `14 passed`.

## Seguimiento web broker operador

- Se alineo el login del turnero para `web_broker`: sin helper/manual visible, sin polling local y con reanudacion del intento pendiente sin disparar un segundo `start`.
- El helper de pruebas ahora conserva `csrfToken` desde `status` hacia `start`, para que los escenarios compartidos entre admin y operador no arranquen anonimos por error.
- El shell de `operador-turnos.html` ahora explica helper local vs broker web sin mezclar ambos caminos como si fueran equivalentes.

## Validacion adicional 2

- `TEST_REUSE_EXISTING_SERVER=1 TEST_LOCAL_SERVER_PORT=8231 npx playwright test tests/queue-operator.spec.js tests/operator-auth-shared-session.spec.js --workers=1 --reporter=line`
- Resultado: `21 passed`.
- `TEST_REUSE_EXISTING_SERVER=1 TEST_LOCAL_SERVER_PORT=8231 npx playwright test tests/admin-openclaw-login.spec.js tests/operator-auth-shared-session.spec.js tests/queue-operator.spec.js --workers=1 --reporter=line`
- Resultado: `31 passed`.

## Nota de gate amplio

- `npm run test:admin:openclaw-auth` sigue fallando por un contrato preexistente y ajeno a este diff: `tests-node/prod-openclaw-auth-rollout-contract.test.js`.
- El fallo apunta a wiring faltante en `MONITOR-PRODUCCION.ps1`, archivo que no cambia en este bloque del lane frontend.
