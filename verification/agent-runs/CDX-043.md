# CDX-043 - Turnero web pilot: backend canon and readiness lane

Estado: `blocked`
Inicio: `2026-03-14`
Estrategia: `STRAT-2026-03-turnero-web-pilot`
Subfrente: `SF-backend-turnero-web-pilot`

## Objetivo

- Sostener el piloto web por clinica desde backend y gates.
- Endurecer `clinic-profile`, `queue state` y `queueSurfaceStatus` como fuente de verdad operativa.
- Dejar `verify-remote` y readiness listos para un corte repetible sin depender de nativo.

## Archivos reservados

- `api.php`
- `controllers/AdminDataController.php`
- `controllers/QueueController.php`
- `lib/TurneroClinicProfile.php`
- `lib/QueueSurfaceStatusStore.php`
- `lib/QueueService.php`
- `bin/turnero-clinic-profile.js`
- `scripts/ops/prod/SMOKE-PRODUCCION.ps1`
- `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1`
- `tests/test_queue_service.php`
- `tests/test_figo_queue_core.php`

## Contrato validado

- El contrato canonico del piloto sigue siendo `turnero-clinic-profile/v1`.
- El perfil activo validado para el corte es `piel-armonia-quito`.
- `/api.php?resource=data` y el carril de readiness quedaron alineados al mismo contexto clinico local.
- La salida remota sigue dependiendo de `/health` con `checks.turneroPilot`, `verify:prod`, `smoke:prod` y `gate:prod`.

## Comandos ejecutados

- `node agent-orchestrator.js status --json`
- `node agent-orchestrator.js task ls --active --json`
- `npx rollup -c rollup.config.mjs`
- `npm run chunks:admin:prune`
- `npm run gate:turnero:web-pilot`
- `node --test tests-node/turnero-clinic-profile-cli.test.js tests-node/health-turnero-pilot-contract.test.js tests-node/prod-ops-public-sync-contract.test.js tests-node/prod-monitor-public-sync-contract.test.js tests-node/api-diagnostics-access-contract.test.js`
- `php vendor/bin/phpunit --no-coverage tests/Integration/HealthVisibilityTest.php`
- `node --test tests-node/queue-pilot-smoke-signal-contract.test.js`
- `npm run lint:js`
- `npm run test:critical:funnel`
- `npm run verify:prod:turnero:web-pilot`
- `npm run smoke:prod:turnero:web-pilot`
- `npm run gate:prod:turnero:web-pilot`
- `node agent-orchestrator.js jobs verify public_main_sync --json`
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/ops/prod/MONITOR-PRODUCCION.ps1 -Domain https://pielarmonia.com`
- `npm run chunks:admin:check`
- `npm run agent:gate`

## Resultado

- El carril local del piloto quedo verde; `npm run gate:turnero:web-pilot` termino `OK`.
- El parser de `scripts/ops/prod/SMOKE-PRODUCCION.ps1` quedo corregido, el enforcement de `verify-remote` ya no acepta `publicHealthRedacted=true` como verificacion valida del piloto, y los contratos locales asociados pasaron (`27/27` en los tests Node del carril perfil/health/prod-ops/monitor).
- `verify:prod:turnero:web-pilot` (`rerun3`) todavia alcanzó a caer por identidad remota redactada: `health-diagnostics` seguia en `403`, el `health` publico remoto no exponia `checks.turneroPilot` / `checks.publicSync`, y el script ya lo reporta como `[FAIL] turnero pilot remote health redacted`.
- Minutos despues, el entorno remoto empeoro a una degradacion mas severa: `verify`, `smoke` y `gate` empezaron a recibir `502` sostenido en `health`, `reviews`, `availability`, `figo-chat.php` y `figo-backend.php`.
- El `gate:prod:turnero:web-pilot` mas reciente (`rerun4`) dejo el bloqueo backend en estado de caida operativa real:
    - `verify` falla con `7` assets rojos, incluyendo `turnero-pilot-remote-verify` y `health-endpoint`
    - `smoke` cae a `12/22` checks HTTP base `OK`
    - `Benchmark API` confirma `25/25` fallas de status para `health`, `reviews`, `availability`, `figo-get` y `figo-post`
- El spot-check directo posterior por `curl` confirma que el host seguia devolviendo `502` para `health`, `reviews`, `availability` y `figo-chat.php`.
- `node agent-orchestrator.js jobs verify public_main_sync --json` ahora deja el cron del host en `verification_source=registry_only`, `verified=false`, `healthy=false`, `failure_reason=unverified`, sin `deployed_commit` ni timestamps.
- `MONITOR-PRODUCCION.ps1` agrega confirmacion operativa del corte: `health-diagnostics`, `reviews`, `availability`, `booked-slots`, `service-priorities` y `figo-get` ya responden `502`.
- La alerta abierta de conectividad en GitHub (`#442`, `[ALERTA PROD] Diagnose host connectivity sin ruta de deploy`) sigue vigente y ahora convive con la caida `502`. El issue reporta `connectivity_status=unreachable`, `configured_host_count=2`, `open_targets=none`.
- Hay fixes recientes en el repo remoto para exponer `publicSync`/health (`696c8b9`, `b248421`), pero no llegaron al host: sus corridas `CI` quedaron `failure`, y los `Deploy Hosting (Canary Pipeline)` asociados (`run 929` y `run 930`) quedaron `skipped`.
- `npm run chunks:admin:check` paso en verde.
- Revalidacion remota del `2026-03-14 17:06` (`America/Guayaquil`) despues de limpiar CI/gobernanza local:
    - `verify:prod:turnero:web-pilot` ya no encuentra outage general; el host vuelve a `200`, pero sigue fallando con `5` bloqueos reales: `health-diagnostics` en `403`, issue de conectividad `#442` abierto, `turnero pilot remote health redacted`, `figo-status mode=degraded` y `figo-status upstreamReachable=false`.
    - `smoke:prod:turnero:web-pilot` confirma recuperacion de superficie (`22/22` checks HTTP base `OK`) pero todavia falla por contrato: `turneroPilot remote health redacted`, `turneroPilot remote mismatch`, alerta GitHub `#442` y `figo` degradado.
    - `gate:prod:turnero:web-pilot` mantiene benchmark sano (`0` status/network failures en `25` samples por endpoint) y falla solo por los bloques `verify + smoke`; ya no por latencia ni por `502`.
    - `MONITOR-PRODUCCION.ps1` confirma el mismo nuevo estado: `home/reviews/availability/booked-slots/service-priorities/figo-get` responden `200`, pero `health-diagnostics` sigue en `403`, `figo` sigue `degraded` y `availability/booked-slots` siguen en `meta.source=fallback`.
    - `node agent-orchestrator.js jobs verify public_main_sync --json` sigue dejando `public_main_sync` en `verification_source=registry_only`, `verified=false`, `healthy=false`, `state=unknown`, `failure_reason=unverified`, sin `deployed_commit` ni timestamps.
- Revalidacion de gobernanza del `2026-03-14 17:09` (`America/Guayaquil`):
    - `node agent-orchestrator.js status --json` deja `policy.revision=699`, `strategy.active=STRAT-2026-03-turnero-web-pilot`, `active_tasks_total=2` y `jobs.public_main_sync.healthy=false`.
    - `node agent-orchestrator.js task ls --active --json` confirma que solo siguen activas/bloqueadas `CDX-043` y `CDX-044`.
    - `npm run agent:gate` ya no es bloqueo local reproducible; vuelve a verde (`503 pass`, `0 fail`).
- Endurecimiento del carril de release del `2026-03-14 17:40` (`America/Guayaquil`):
    - `prod-monitor`, `post-deploy-fast`, `post-deploy-gate`, `deploy-hosting`, `deploy-frontend-selfhosted` y `repair-git-sync` ahora exportan `PIELARMONIA_DIAGNOSTICS_ACCESS_TOKEN` / `PIELARMONIA_CRON_SECRET` y header/prefix al entorno del workflow.
    - Eso habilita que `Common.Http.ps1` y `bin/turnero-clinic-profile.js verify-remote` usen `health-diagnostics` autenticado cuando el secreto exista en GitHub Actions, en vez de degradarse siempre al `health` publico.
    - Los contratos de workflow del cambio pasan (`53/53`) y la regresion completa de gobernanza sigue verde: `npm run agent:test` queda en `508 pass`, `0 fail`, y `npm run agent:gate` vuelve a verde con espejo Codex y validacion PHP intactos.
    - Aun no se puede afirmar que el problema remoto quedo resuelto: falta rerun real en Actions o entorno con secreto cargado para comprobar si la redaccion actual viene solo de credencial ausente o de host desactualizado.
- Revalidacion adicional el `2026-03-14 09:07` (`America/Guayaquil`):
    - `verify:prod:turnero:web-pilot` sigue en rojo con `7` fallas.
    - `smoke:prod:turnero:web-pilot` vuelve a caer en `12/22` checks HTTP base `OK`, con `502` sostenido en `health`, `reviews`, `availability`, `funnel`, `admin auth`, `figo`, `backup receiver` y el `POST` de `figo chat`.
    - `MONITOR-PRODUCCION.ps1` confirma otra vez `502` en `health-diagnostics`, `reviews`, `availability`, `booked-slots`, `service-priorities` y `figo-get`.
    - El host no recupero identidad remota del piloto: `clinicId=` vacio, `fingerprint=` vacio y `catalogReady=False`.
    - `node agent-orchestrator.js jobs verify public_main_sync --json` sigue en `verified=false`, `healthy=false`, `state=unknown`, `failure_reason=unverified`.
    - La inspeccion de GitHub Actions no muestra despliegues nuevos sanos: para `b248421` siguen `CI`, `Agent Governance` y `Post-Deploy Fast Lane` en `failure`, mientras `Deploy Hosting (Canary Pipeline)` aparece `skipped`.
- Diagnostico de por que `b248421` no llego al host:
    - `CI` fallo por una combinacion de problemas previos al deploy:
        - `Lint JS`: errores en `src/apps/admin-v3/shared/modules/queue/runtime/mode.js` (`no-extra-boolean-cast`) y parseo roto en `tests-node/queue-pilot-smoke-signal-contract.test.js`.
        - `Run PHPUnit with Coverage`: `TelemedicineLegacyBridgeTest` rompia por `/usr/sbin/sendmail: not found`, y `LeadOpsEndpointsTest` fallaba porque esperaba `Botox medico` pero recibia `Rejuvenecimiento`.
        - `Run Critical Funnel Gate`: `tests/funnel-tracking.spec.js` seguia esperando copy/CTA anteriores (`Even with online booking paused...`, `Open telemedicine`, `Dermatology services`).
    - `Agent Governance` fallo en ese commit porque `php-governance-contract` detectaba drift entre `PLAN_MAESTRO_CODEX_2026.md` y `AGENT_BOARD.yaml` (`CODEX_STRATEGY_ACTIVE` sin `strategy.active` activa en el board), y `workspace-hygiene-contract` todavia reclamaba `.tmp-calendar-write-report.json` y `styles.min.css` como archivos archivados faltantes.
    - `Post-Deploy Fast Lane` no alcanzo a destrabar el host: en esa corrida `verify` seguia cayendo por `health` `403`, identidad remota del piloto sin resolver y `figo-status` degradado; ademas, el `smoke` de esa revision moria por `ParserError` en `scripts/ops/prod/SMOKE-PRODUCCION.ps1`.
- Contraste con el workspace local actual:
    - `npx eslint src/apps/admin-v3/shared/modules/queue/runtime/mode.js tests-node/queue-pilot-smoke-signal-contract.test.js` pasa en verde.
    - `node --test tests-node/php-governance-contract.test.js tests-node/workspace-hygiene-contract.test.js` pasa en verde (`34/34`).
    - `npx playwright test tests/funnel-tracking.spec.js --workers=1` pasa en verde (`3 passed`).
    - `vendor/bin/phpunit tests/Integration/LeadOpsEndpointsTest.php tests/Integration/TelemedicineLegacyBridgeTest.php` ya no reproduce el error/failure funcional; solo deja warning del runner por coverage (`XDEBUG_MODE=coverage`).
    - `smoke:prod:turnero:web-pilot` ya no rompe por parser localmente; el bloqueo actual es el `502` remoto, no el script.
- La deuda local reproducible de CI que venia frenando esos deploys ya no muestra rojos de codigo: `HealthVisibilityTest` pasa (`6 tests, 58 assertions`), `queue-pilot-smoke-signal-contract` pasa (`3/3`), `npm run lint:js` queda en `0 errors` y `npm run test:critical:funnel` vuelve a verde (`6 passed`, `1 skipped`).
- Revalidacion local exacta del `2026-03-14 09:16` (`America/Guayaquil`) contra los jobs que bloquearon `b248421`:
    - `npm run lint:js` completa con `0 errors, 172 warnings`.
    - `npm run test:critical:funnel` completa en verde (`6 passed`, `1 skipped`).
    - `npm run agent:test` completa en verde (`503 pass`, `0 fail`).
    - `vendor/bin/phpunit --no-coverage tests/Integration/LeadOpsEndpointsTest.php tests/Integration/TelemedicineLegacyBridgeTest.php` completa en verde (`9 tests, 69 assertions`).
    - La combinacion `lint + funnel + governance + phpunit subset` deja al workspace en estado publicable desde el punto de vista local; lo que sigue bloqueando el release es el remoto: release drift, `health` publico redactado, `public_main_sync` sin verificar y `figo` degradado.
- Revalidacion remota del `2026-03-14 17:44` -> `17:48` (`America/Guayaquil`) despues del wiring de diagnostics:
    - Esta shell local sigue sin `PIELARMONIA_DIAGNOSTICS_ACCESS_TOKEN` / `PIELARMONIA_CRON_SECRET`, asi que el rerun directo continua viendo `health-diagnostics=403` y no invalida todavia la hipotesis de que GitHub Actions con secreto si pueda ver la identidad completa del piloto.
    - `verify:prod:turnero:web-pilot` (`rerun6`) deja `14` fallas y ya no solo marca drift del piloto: reaparece drift del shell publico con `index remoto contiene scripts inline ejecutables`, mismatch de hash en `styles.css` y `public-v6-shell.js`, y faltantes de marcadores/eventos GA4 (`initGA4`, `payment_success`, `booking_confirmed`, `chat_handoff_whatsapp`, `whatsapp_click`).
    - `smoke:prod:turnero:web-pilot` (`rerun6`) mantiene `22/22` checks HTTP base `OK`, pero sigue fallando por contrato con `turneroPilot remote health redacted`, `turneroPilot remote mismatch`, issue GitHub `#442` abierto y `figo` degradado.
    - `gate:prod:turnero:web-pilot` (`rerun6`) confirma que el benchmark vuelve a estar sano; falla por `verify + smoke`, no por latencia ni por outage general.
    - El bloqueo backend ya no es solo `/health` redactado: tambien queda evidencia nueva de release drift en el shell publico desplegado, ademas de `public_main_sync` sin verificar y `figo-status mode=degraded`.
- Revalidacion de gobernanza del `2026-03-14` posterior al rerun6:
    - `node agent-orchestrator.js status --json` deja `policy.revision=701`, `strategy.active=STRAT-2026-03-turnero-web-pilot` y `jobs.public_main_sync.healthy=false`.
    - `node agent-orchestrator.js task ls --active --json` ya no muestra `CDX-044` en `in_progress`: queda `CDX-043=blocked` y `CDX-044=review` por mutacion concurrente ajena a esta corrida.
    - La estrategia sigue `active`; no se cierran tareas ni se cambia el canon.
- Revalidacion local final del `2026-03-14` posterior a sincronizar evidencia:
    - `npm run agent:gate` detecto transitoriamente un chunk admin huerfano (`index-MyvPG610.js`) fuera del grafo activo.
    - `npm run chunks:admin:prune` limpio ese residuo y `npm run agent:gate` (`rerun4` y `rerun5`) volvio y se mantuvo en verde.
    - El incidente no cambia el diagnostico del piloto: el bloqueo de A2.0 sigue siendo remoto.
- Se dejo ademas un manifiesto de corte limpio/publicable para no mezclar el piloto con el resto del worktree: `verification/agent-runs/turnero-web-pilot-release-slice-20260314.md`.
- Revalidacion operativa del `2026-03-17 04:10` -> `04:18` (`America/Guayaquil`) para destrabar `public_main_sync`:
    - Baseline local antes de tocar Actions:
        - `node agent-orchestrator.js status --json` mantiene `policy.revision=713`, `strategy.active=STRAT-2026-03-turnero-web-pilot`, `CDX-043=blocked`, `CDX-044=review` y `jobs.public_main_sync.healthy=false`.
        - `node agent-orchestrator.js jobs verify public_main_sync --json` sigue en `verification_source=health_url`, `verified=false`, `healthy=false`, `failure_reason=health_missing_public_sync`, `deployed_commit=''`.
        - `curl https://pielarmonia.com/api.php?resource=health` sigue respondiendo `{"ok":true,"status":"ok",...}` sin bloque `checks`, con `version=20260314072914`.
        - `gh auth status` confirma CLI autenticado con scopes `repo` y `workflow`, suficiente para disparar recovery sobre `main`.
    - Se disparo `repair-git-sync.yml` manual sobre `main` (`run 23186705104`):
        - El workflow alcanzo a completar `Forzar sincronizacion git remota` y `Esperar propagacion de cache`, pero termino `failure` porque `Verificar produccion despues de repair` y `Smoke rapido post-repair` quedaron rojos.
        - La metadata visible del job no pudo confirmar el resultado fino del host-side publish: `host_cron_wrapper_sync_state=unknown`, `host_public_deploy_state=unknown`, `host_public_deploy_reason=ssh_stdout_unavailable`.
        - Rerun local inmediato despues del repair no muestra cambio: `jobs verify public_main_sync --json` sigue en `health_missing_public_sync` y el `health` publico sigue sin `checks.publicSync`.
    - Se disparo luego `deploy-hosting.yml` por transporte sobre `main` (`run 23186870666`) con `force_transport_deploy=true`, `protocol=ftps`, `require_staging_canary=false`, `allow_prod_without_staging=true`, `run_postdeploy_fast=false`, `run_postdeploy_gate=false`, `skip_public_conversion_smoke=true`.
        - `Deploy Canary (Staging)` no bloqueo: el workflow aplico el override `allow_prod_without_staging=true` ante secretos `STAGING_FTP_*` ausentes.
        - `Deploy Production` fallo en `Preflight red (Prod)` con `transport_preflight_reason=runner_tcp_unreachable` y `transport_preflight_target=ftps:21`; no hubo upload por runner GitHub-hosted.
        - El mismo run resolvio localmente el perfil activo del piloto como `clinic_id=piel-armonia-quito`, `profileFingerprint=0208f138`, `release_mode=suite_v2`, pero el remoto sigue `blocked` con `remote_clinic_id_missing`, `remote_profile_fingerprint_missing`, `remote_catalog_not_ready` y `remote_deployed_commit_missing`.
        - El workflow abrio la incidencia `#455` (`[ALERTA PROD] Deploy Hosting transporte bloqueado desde GitHub Runner`) y disparo `diagnose-host-connectivity.yml` (`run 23186914984`), que termino `success`.
    - Verificacion local adicional posterior a ambos workflows:
        - `powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1 -Domain https://pielarmonia.com` confirma que el `health` publico sigue sin `checks.publicSync`, `health-diagnostics` sigue `403`, y `figo-status` sigue `mode=degraded` con `upstreamReachable=false`.
        - Esta shell local no tiene `SSH_HOST`, `SSH_USERNAME`, `SSH_PASSWORD`, `FTP_SERVER`, `FTP_USERNAME` ni `FTP_PASSWORD`; por eso no fue posible ejecutar desde aqui el ultimo paso del runbook (`bash ./bin/deploy-public-v3-live.sh` en el VPS).
    - Estado resultante del corte:
        - `public_main_sync` no quedo reparado en esta corrida.
        - El bloqueo ya no es ambiguo: despues de `repair-git-sync` fallido y del fallback FTPS bloqueado, el siguiente paso real es `publish` manual en el host con credenciales del VPS, seguido por rerun de `VERIFICAR-DESPLIEGUE.ps1` y `node agent-orchestrator.js jobs verify public_main_sync --json`.

## Decision de release

- A2.0 no se cierra.
- `CDX-043` queda en `blocked` hasta que el remoto exponga identidad valida del piloto (`checks.turneroPilot` y `checks.publicSync` o `health-diagnostics` autorizado), `public_main_sync` vuelva a quedar verificable, `figo` salga de `degraded`, se cierre la incidencia de conectividad `#442`, y el shell publico desplegado vuelva a coincidir con los hashes y markers esperados del corte local.
- El bloqueo de `public_main_sync` queda acotado al publish manual del VPS y a la recuperacion de la ruta de transporte GitHub-hosted (`issue #455`); no hay evidencia nueva de regresion local en `main`.
- No se cambia el canon clinico ni se agrega decision nueva al `decision_log.md`.

## Evidencia

- `verification/agent-runs/gate-turnero-web-pilot-20260314.log`
- `verification/agent-runs/verify-prod-turnero-web-pilot-20260314.log`
- `verification/agent-runs/smoke-prod-turnero-web-pilot-20260314.log`
- `verification/agent-runs/gate-prod-turnero-web-pilot-20260314.log`
- `verification/agent-runs/verify-prod-turnero-web-pilot-20260314-rerun2.log`
- `verification/agent-runs/smoke-prod-turnero-web-pilot-20260314-rerun2.log`
- `verification/agent-runs/gate-prod-turnero-web-pilot-20260314-rerun3.log`
- `verification/agent-runs/verify-prod-turnero-web-pilot-20260314-rerun3.log`
- `verification/agent-runs/smoke-prod-turnero-web-pilot-20260314-rerun3.log`
- `verification/agent-runs/gate-prod-turnero-web-pilot-20260314-rerun4.log`
- `verification/agent-runs/monitor-prod-20260314-rerun1.log`
- `verification/agent-runs/verify-prod-turnero-web-pilot-20260314-rerun4.log`
- `verification/agent-runs/smoke-prod-turnero-web-pilot-20260314-rerun4.log`
- `verification/agent-runs/monitor-prod-20260314-rerun2.log`
- `verification/agent-runs/health-visibility-20260314-rerun1.log`
- `verification/agent-runs/queue-pilot-smoke-signal-contract-20260314-rerun1.log`
- `verification/agent-runs/lint-js-20260314-rerun1.log`
- `verification/agent-runs/test-critical-funnel-20260314-rerun1.log`
- `verification/agent-runs/lint-js-20260314-rerun2.log`
- `verification/agent-runs/test-critical-funnel-20260314-rerun2.log`
- `verification/agent-runs/agent-test-20260314-rerun1.log`
- `verification/agent-runs/phpunit-leadops-telemedicine-20260314-rerun1.log`
- `verification/agent-runs/verify-prod-turnero-web-pilot-20260314-rerun5.log`
- `verification/agent-runs/smoke-prod-turnero-web-pilot-20260314-rerun5.log`
- `verification/agent-runs/gate-prod-turnero-web-pilot-20260314-rerun5.log`
- `verification/agent-runs/monitor-prod-20260314-rerun3.log`
- `verification/agent-runs/public-main-sync-20260314-rerun2.json`
- `verification/agent-runs/health-public-20260314-rerun1.json`
- `verification/agent-runs/figo-status-20260314-rerun1.json`
- `verification/agent-runs/agent-status-20260314-rerun1.json`
- `verification/agent-runs/task-ls-active-20260314-rerun1.json`
- `verification/agent-runs/chunks-admin-check-20260314-rerun1.log`
- `verification/agent-runs/agent-gate-20260314-rerun1.log`
- `verification/agent-runs/workflow-diagnostics-auth-contract-20260314-rerun1.log`
- `verification/agent-runs/agent-test-20260314-rerun2.log`
- `verification/agent-runs/agent-gate-20260314-rerun2.log`
- `verification/agent-runs/repair-git-sync-diagnostics-auth-20260314-rerun1.json`
- `verification/agent-runs/verify-prod-turnero-web-pilot-20260314-rerun6.log`
- `verification/agent-runs/smoke-prod-turnero-web-pilot-20260314-rerun6.log`
- `verification/agent-runs/gate-prod-turnero-web-pilot-20260314-rerun6.log`
- `verification/agent-runs/agent-status-20260314-rerun3.json`
- `verification/agent-runs/task-ls-active-20260314-rerun3.json`
- `verification/agent-runs/agent-gate-20260314-rerun4.log`
- `verification/agent-runs/agent-gate-20260314-rerun5.log`
- `verification/agent-runs/turnero-web-pilot-release-slice-20260314.md`
