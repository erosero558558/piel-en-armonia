# AG-226 - Extraer helper compartido para mocks de queue-display

## Resumen

- Creado `tests/helpers/turnero-surface-mocks.js` para centralizar mocks reutilizables de `clinic-profile.json` y `queue-state`.
- Migrado `tests/queue-display.spec.js` para usar el helper compartido en escenarios OK, 404, abort y payload dinámico.
- Agregado contrato Node en `tests-node/turnero-surface-mocks-helper.test.js`.

## Archivos

- `tests/helpers/turnero-surface-mocks.js`
- `tests/queue-display.spec.js`
- `tests-node/turnero-surface-mocks-helper.test.js`

## Verificacion

- `node --test tests-node/turnero-surface-mocks-helper.test.js`
    - Resultado: `5/5` verde.
- `npx eslint tests/helpers/turnero-surface-mocks.js tests-node/turnero-surface-mocks-helper.test.js tests/queue-display.spec.js`
    - Resultado: limpio, sin errores ni warnings nuevos.
- `$env:TEST_REUSE_EXISTING_SERVER='1'; npx playwright test tests/queue-display.spec.js --workers=1`
    - Resultado: `12/12` verde.

## Notas

- El helper quedó deliberadamente pequeño para reusar después en `queue-kiosk` y otros specs del turnero sin mezclar todavía recursos distintos a `queue-state`.
- El warning residual de `public_main_sync` sigue siendo ajeno a este bloque (`status_file_missing` / `telemetry_gap`).
