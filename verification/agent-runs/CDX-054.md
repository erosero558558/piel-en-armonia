# CDX-054

## Resumen

- Slice de rescate canonico para `queue/turnero` sobre `origin/main`.
- Objetivo: mover `Cola y pantalla` y `Asistente de orientacion` fuera de
  `#kioskSupportShell`, manteniendo el shell solo para soporte del equipo.

## Fuentes

- Donor worktree validado:
  `/home/deck/Documents/GitHub/piel-en-armonia-queue-kiosk-clean/.codex-worktrees/CDX-054`
- Patchset de rescate:
  `/tmp/piel-rescue-2026-03-27/queue-kiosk-visibility.patch`
- Carryover `public-v6` preservado aparte y fuera de esta slice.

## Estado inicial

- Estrategia activa: `STRAT-2026-03-admin-operativo`
- Foco activo: `feedback_trim`
- Lineage: `AG-260`

## Implementacion

- Se portara solo el hunk funcional de `kiosco-turnos.html`.
- `Cola y pantalla` y `Asistente de orientacion` quedan visibles por defecto
  en el `aside` principal.
- `#kioskSupportShell` queda reservado al bloque operativo del equipo.
- Se preservan los IDs canonicos del kiosco:
  `#kioskSupportShell`, `#queueWaitingCount`, `#queueConnectionState`,
  `#queueUpdatedAt`, `#assistantForm`, `#assistantMessages`.

## Desviacion controlada

- El donor tambien incluia `queue-kiosk.css`, pero esta publicacion no lo
  porta para no abrir conflicto de lane fuera de `frontend_content`.
- El ajuste visual de `queue-kiosk.css` queda preservado en
  `/tmp/piel-rescue-2026-03-27/queue-kiosk-visibility.patch` para una slice
  futura si hiciera falta.

## Verificacion

- Pendiente de correr en el worktree canonico:
  `TEST_LOCAL_SERVER_PORT=9114 TEST_REUSE_EXISTING_SERVER=0 npx playwright test tests/queue-kiosk.spec.js --workers=1`

## Estado

- Bootstrap canonico listo.
- Pendiente solo portar el HTML funcional, validar Playwright y ejecutar
  `review/close`.
