# AG-026 - Agent Autopilot workflow hardening (local)

## Goal
Mejorar robustez y diagnostico del workflow `agent-autopilot` para fallas de permisos/validacion en CI.

## Implementado
- `.github/workflows/agent-autopilot.yml`
  - Permisos explicitos para pasos que consultan/leen APIs usadas por `intake/reconcile`:
    - `issues: read`
    - `actions: read`
    - `pull-requests: read`
  - Separacion de validaciones en pasos con diagnostico mas claro
  - Snapshot JSON en fallo para facilitar analisis posterior

## Validacion local
- Parse YAML:
  - `.github/workflows/agent-autopilot.yml` -> OK

## Validacion remota
- Run `Agent Autopilot` manual: `22384295981` -> **success**
- Confirmado en remoto:
  - `Intake and queue sync` -> success
  - `Governance validation` -> success
  - `Commit queue state changes` -> success (genero commit de sync en la rama de validacion)

## Pendiente externo/manual
- Rerun en `main` para cerrar la alerta asociada (`workflow:agent-autopilot:main`) con evidencia en la linea principal.
