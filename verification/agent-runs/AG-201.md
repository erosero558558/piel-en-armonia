# AG-201 - Public main sync telemetry propagation

- Date: `2026-03-11`
- Task title: `Public main sync telemetry propagation`

## Outcome

- `lib/public_sync.php` now propagates the cron runtime telemetry already written by `bin/deploy-public-v3-cron-sync.sh` into `checks.publicSync`, including `repoPath`, `branch`, `currentHead`, `remoteHead`, `durationMs`, `dirtyPathsCount`, `dirtyPathsSample`, and `dirtyPaths`.
- `tools/agent-orchestrator/domain/jobs.js` now normalizes that telemetry from both `local_status_file` and `health_url` verification modes, so `jobs status`, `jobs verify`, and `status --json` expose the same actionable fields without breaking the existing contract.
- `docs/AGENT_ORCHESTRATION_RUNBOOK.md` now documents the concrete triage path for `public_main_sync` failures: compare `currentHead` vs `remoteHead` and use `dirtyPathsCount` / `dirtyPathsSample` when `lastErrorMessage=working_tree_dirty`.

## Verification

- `node --test tests-node/orchestrator/jobs-engine.test.js tests-node/agent-jobs-cli.test.js` -> `PASS` (`5/5`)
- `php -l lib/public_sync.php` -> `No syntax errors detected`
- `node agent-orchestrator.js board doctor --json` -> `OK` (sin findings bloqueantes)

## Operational Note

- `node agent-orchestrator.js status --json` still reports `warn.jobs.public_main_sync_failed` on `2026-03-11` with `last_error_message=working_tree_dirty`.
- That remaining warning reflects the current live host state reached through `health_url`; the new telemetry becomes fully visible in production once this repo state is deployed and the host runs the updated code.
