# S7-25 — Validar `release/`

Fecha: 2026-03-30

## Resumen

- Se auditó `release/turnero-apps-pilot-local`.
- No aparecieron secretos con:

```bash
grep -rn 'API_KEY\|password\|secret\|sk_live' release/ 2>/dev/null
```

- El bundle local contiene 7 archivos esperados:
  - `app-downloads/pilot/release-manifest.json`
  - `app-downloads/pilot/SHA256SUMS.txt`
  - `app-downloads/pilot/operator/win/TurneroOperadorSetup.exe`
  - `app-downloads/pilot/operator/win/TurneroOperadorSetup.exe.blockmap`
  - `desktop-updates/pilot/operator/win/latest.yml`
  - `desktop-updates/pilot/operator/win/TurneroOperadorSetup.exe`
  - `desktop-updates/pilot/operator/win/TurneroOperadorSetup.exe.blockmap`

## Validación realizada

Comandos ejecutados:

```bash
find release -type f
grep -rn 'API_KEY\|password\|secret\|sk_live' release/ 2>/dev/null
shasum -a 256 \
  release/turnero-apps-pilot-local/app-downloads/pilot/operator/win/TurneroOperadorSetup.exe \
  release/turnero-apps-pilot-local/desktop-updates/pilot/operator/win/TurneroOperadorSetup.exe
cat release/turnero-apps-pilot-local/app-downloads/pilot/SHA256SUMS.txt
```

Resultados:

- El `.exe` publicado en `app-downloads/` y `desktop-updates/` comparte el hash:
  `42be7ebb5bf5442d5df8f51c4dbbd4ffac6ee584a45c36038115bf5ed95ed5cf`
- `SHA256SUMS.txt` cubre instalador, `blockmap` y `latest.yml`.
- `release-manifest.json` coincide con bytes y `sha256` reales de los 5 archivos
  publicados para `operator/win`.

## Riesgo residual

- `npm run turnero:verify:pilot:local` falla hoy porque falta
  `bin/verify-turnero-release-bundle.js`.
- Se dejó fallback manual documentado en
  `docs/RUNBOOK_TURNERO_APPS_RELEASE.md` para no publicar a ciegas mientras se
  repone ese script.
