# AG-213 - Remove github-script dependency from diagnose-host-connectivity

## Summary

- Replaced the issue-management steps in `.github/workflows/diagnose-host-connectivity.yml` so they no longer depend on `actions/github-script@v7`.
- Switched the incident create/close logic to `shell: bash` + native `node` + `fetch`, using `github.token` and `github.api_url`.
- Fixed the inline `node` execution by wrapping both scripts in async IIFEs so `await` is valid in the heredoc context.

## Validation

- `node --test tests-node/deploy-hosting-workflow-contract.test.js`
  - Result: `11/11` passing locally.
- Published commit to `main` from clean worktree:
  - `1d457ed3 remove github-script from connectivity diagnosis`
  - `22dfe730 fix connectivity incident inline script`
- GitHub Actions:
  - `Diagnose Host Connectivity` run `22988991041` on `22dfe730` completed successfully.
  - `Diagnose Host Connectivity` run `22989166634` (triggered from repair) completed successfully.

## Outcome

- The workflow no longer fails during `Set up job` because of `actions/github-script` download/auth issues.
- The incident-management steps now execute successfully end-to-end in GitHub Actions.
- Remaining deploy/page blockers are outside this task and still relate to host transport / self-hosted runner availability.
