# CDX-063

## Resumen

- Slice `codex_transversal` de soporte abierta despues de arrancar la fase
  multi-clinica, porque el frente necesitó seed/focus canonicos y resolver
  required checks desde evidencia local.
- No toca producto `turnero`, deploy ni `public_main_sync`; solo regulariza
  board/plan/orquestador para que `CDX-061` y `CDX-062` queden bien
  representadas.

## Write Set

- `AGENT_BOARD.yaml`
- `PLAN_MAESTRO_CODEX_2026.md`
- `tools/agent-orchestrator/domain/strategy.js`
- `tools/agent-orchestrator/domain/focus.js`
- `tests-node/orchestrator/focus.test.js`
- `verification/agent-runs/CDX-063.md`

## Cambios aplicados

- Se agrega el seed `turnero-web-pilot-multi-clinic-local` al catalogo de
  estrategia del orquestador.
- Se agrega el focus
  `FOCUS-2026-03-turnero-web-pilot-multi-clinic-cut-1` con los mismos tres
  required checks del frente `turnero` local-first.
- El subfrente backend multi-clinica pasa a tratar `scope=turnero` como
  `support_only_scope`, para que `CDX-062` deje de quedar huerfana.
- Board y plan quedan alineados con `CDX-063` como soporte transversal real
  y con los artefactos regenerados (`admin.js`, `js/queue-operator.js`)
  absorbidos por `CDX-061`.

## Validacion

```bash
node --test tests-node/orchestrator/focus.test.js --test-name-pattern "turnero multi-clinica|turnero local-first"
node agent-orchestrator.js strategy preview --seed turnero-web-pilot-multi-clinic-local --json
node agent-orchestrator.js focus verify --json
node agent-orchestrator.js leases heartbeat CDX-061 --ttl-hours 4 --expect-rev 824 --json
node agent-orchestrator.js leases heartbeat CDX-062 --ttl-hours 4 --expect-rev 825 --json
node agent-orchestrator.js leases heartbeat CDX-063 --ttl-hours 4 --expect-rev 826 --json
node agent-orchestrator.js status --json
node agent-orchestrator.js task ls --active --json
node agent-orchestrator.js codex-check --json
node agent-orchestrator.js board doctor --json --profile ci
node agent-orchestrator.js handoffs lint --json
```

Resultado:

- El seed `turnero-web-pilot-multi-clinic-local` ya previewa en verde.
- El foco multi-clinica materializa snapshot local valido en
  `verification/focus-checks/FOCUS-2026-03-turnero-web-pilot-multi-clinic-cut-1.json`.
- `task ls --active --json` queda exactamente con `CDX-061`, `CDX-062` y
  `CDX-063`, todas en `review`.
- `board doctor --json --profile ci` ya no encuentra tareas huerfanas ni
  leases faltantes.
- `handoffs lint --json` queda `ok=true`; solo persiste el warning externo de
  `public_main_sync`.
- `codex-check --json` deja de fallar por estrategia/focus y queda bloqueado
  solo por canonicidad del workspace: el donor sigue siendo `board_fork`
  frente a `/Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm` y,
  mientras convivan frontend/backend/transversal dirty en un solo worktree,
  `workspace truth` lo clasifica como `mixed_lane_authored`.

## Notas

- `CDX-063` existe porque la ola multi-clinica ya no cabia solo en
  frontend/backend; no reabre una tercera ola funcional.
- Esta slice deja listo el frente para que los required checks se consuman
  por evidencia local sin inventar IDs nuevos.
- El siguiente paso operativo ya no es de producto: es separar lanes o
  promover este donor como canon sin arrastrar el drift local del workspace
  raiz.

## Addendum 2026-03-28

- Se realinean las filas activas `CDX-057` y `CDX-100` al frente
  `STRAT-2026-03-turnero-web-pilot-multi-clinic-local`, reemplazando el
  arrastre residual de `STRAT-2026-03-admin-operativo`.
- Con esa reconciliacion, `codex-check --json` vuelve a `ok=true` y
  `board doctor --json` deja `workspace_truth.ok=true`, sin `mixed_lane`,
  `out_of_scope` ni `orphan_tasks`.
- La deuda restante despues de este closeout ya no es de gobernanza del
  board: queda solo reaplicar el delta backend de `CDX-100` sobre un
  worktree limpio para publicar el runner local en `node` y el gate
  `test:turnero:ui` separado de Sony.

## Addendum 2026-03-28 22:15Z

- Se refresca el snapshot local de required checks de `CDX-063` con el
  fingerprint vigente del worktree, manteniendo `contracts` y `php-contract`
  en verde con rerun real.
- El wrapper canónico `npm run test:turnero:web-pilot:ui` hoy sigue
  apuntando a `tests/admin-queue.spec.js`, archivo ya retirado de esta base;
  por eso el reporte falla por `No tests found` solo en la suite `Admin queue`.
- Para validar el gate UI sin reabrir un frente funcional desde la slice
  transversal, se corre el pack equivalente split de admin queue:
  `tests/admin-queue-guidance-live-ops.spec.js`,
  `tests/admin-queue-ops-hub.spec.js`,
  `tests/admin-queue-controls-numpad.spec.js`,
  `tests/admin-queue-pilot-governance.spec.js` y
  `tests/admin-queue-reception-escalation.spec.js`, con resultado
  `88 passed (6.4m)`.
- En el mismo rerun de `npm run test:turnero:web-pilot:ui`, las suites
  `Queue kiosk`, `Queue operator`, `Queue display` y `Queue integrated flow`
  pasan; el unico rojo restante es el path legado del wrapper.
- El ajuste del wrapper y de `package.json` queda explicitamente delegado a
  `CDX-100`; este addendum solo deja trazada la equivalencia de validacion
  necesaria para cerrar la reconciliacion de gobernanza.
