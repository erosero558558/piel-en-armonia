# AG-019 - Audit local (task spec stale / requiere re-scope)

## Goal
Validar si la tarea AG-019 sigue apuntando a archivos reales antes de implementar consolidacion de bundles.

## Hallazgo principal
La especificacion actual de `AG-019` en `AGENT_BOARD.yaml` referencia archivos que ya no existen:

- `src/bundles/data-engine.js` -> **no existe**
- `src/bundles/data-bundle.js` -> **no existe**

El estado real del repo hoy:

- `src/bundles/data.js` -> **existe** (bundle canonico actual)
- `js/engines/data-engine.js` -> **existe** (asset generado / legacy runtime)

## Comandos ejecutados (local)
- `Test-Path src/bundles/data-engine.js`
- `Test-Path src/bundles/data-bundle.js`
- `Test-Path src/bundles/data.js`
- `Test-Path js/engines/data-engine.js`
- `Get-ChildItem src/bundles -File`

## Resultado
- Tarea **no ejecutada** porque el prompt/paths estan desactualizados y puede inducir cambios incorrectos.
- Recomendacion: re-scope AG-019 para auditar overlap real entre `src/bundles/data.js` y consumidores/runtime (`js/engines/data-bundle.js` generado, `js/engines/data-engine.js` legacy si sigue usado).

## Pendiente externo/manual
- Ninguno (solo decision de backlog / board).

