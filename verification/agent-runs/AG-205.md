# AG-205 - Prod monitor public sync incident triage

## Summary

- extended `scripts/ops/prod/MONITOR-PRODUCCION.ps1` so the production monitor now classifies `checks.publicSync` with canonical runtime signals: `failureReason`, `lastErrorMessage`, `currentHead`, `remoteHead`, `headDrift`, `dirtyPathsCount`, `dirtyPathsSample`, and `telemetryGap`
- added the explicit monitor override `-AllowDegradedPublicSync` so operators can keep the monitor in observation mode without hiding the incident diagnosis
- updated `scripts/ops/prod/README.md` and `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md` so monitor, smoke, verify, and weekly report point to the same `public_main_sync` triage model
- added `tests-node/prod-monitor-public-sync-contract.test.js` to lock the monitor/runtime-doc contract

## Verification

- `node --test tests-node/prod-monitor-public-sync-contract.test.js tests-node/prod-ops-public-sync-contract.test.js tests-node/weekly-report-script-contract.test.js`
    - result: `13/13` passing
- PowerShell parser on `scripts/ops/prod/MONITOR-PRODUCCION.ps1`
    - result: no syntax errors

## Residual status

- the live host issue remains operational, not repo-side: `public_main_sync` is still reported unhealthy remotely with `failure_reason=working_tree_dirty`
- this task closes the observability gap in the production monitor so the incident is now classified consistently across monitor, smoke, verify, and weekly report
