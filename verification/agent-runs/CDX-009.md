# CDX-009 - Public sync incident recovery and Windows/self-hosted hardening

## Objetivo

Recuperar `public_main_sync`, confirmar el host productivo real y endurecer el
triage para `windows_selfhosted` sin cambiar el contrato publico de `health`.

## Ventana verde observada

Ventana revalidada el `2026-03-26` entre `04:53:38Z` y `04:54:32Z`.

- `node agent-orchestrator.js jobs verify public_main_sync --json`
    - `verification_source=health_url`
    - `verified=true`
    - `healthy=true`
    - `operationally_healthy=true`
    - `state=idle`
    - `age_seconds=54`
    - `checked_at=2026-03-26T04:53:38.7896399+00:00`
    - `last_success_at=2026-03-25T23:53:34.6219328-05:00`
    - `currentHead=remoteHead=798d6b4ea725e8e3f965000064a1512b60c9f656`
    - `headDrift=false`
    - `telemetryGap=false`
    - `dirtyPathsCount=0`
    - `failureReason=""`
    - `jobId=8d31e299-7e57-4959-80b5-aaa2d73e9674`
- Telemetria viva del target actual
    - `repo_path=C:\dev\pielarmonia-clean-main`
    - `status_path=C:\ProgramData\Pielarmonia\hosting\public-sync-status.json`
    - `log_path=C:\ProgramData\Pielarmonia\hosting\main-sync.runtime.log`
    - `lock_file=C:\tmp\sync-pielarmonia.lock`
- `node agent-orchestrator.js status --json`
    - `policy.revision=754`
    - `job:public_main_sync=green`
    - `runtime:operator_auth=green`
    - `required_checks_ok=true`
    - `release_ready=true`
- `node agent-orchestrator.js runtime verify google_oauth --json`
    - `runtime:operator_auth=green`
    - `figo_queue=healthy`
    - `leadops_worker=degraded(worker_disabled)` sigue fuera de scope

La ventana verde confirmo dos cosas utiles: la ruta productiva real hoy es
`windows_selfhosted`, y el hardening repo-side no rompe el contrato publico de
`checks.publicSync`.

## Hardening repo-side publicado

El hardening operativo quedo publicado en `main` con
`2ba6ad5e681a127d5c70a84ff2cbb35c3994e11c`
(`chore: harden public main sync windows recovery`).

Cambios aplicados:

- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`
  - promueve `jobs verify public_main_sync --json` como discovery obligatorio;
  - declara `windows_selfhosted` como target primario actual; y
  - deja `linux_legacy` como fallback explicito.
- `scripts/ops/prod/CHECKLIST-HOST-PUBLIC-SYNC.ps1`
  - agrega `-HostProfile windows_selfhosted|linux_legacy`;
  - usa `windows_selfhosted` como default efectivo con
    `RepoPath=C:\dev\pielarmonia-clean-main`,
    `StatusPath=C:\ProgramData\Pielarmonia\hosting\public-sync-status.json`,
    `LogPath=C:\ProgramData\Pielarmonia\hosting\main-sync.runtime.log` y
    `LockPath=C:\tmp\sync-pielarmonia.lock`; y
  - preserva los comandos canonicos Linux bajo `linux_legacy`.

Validacion repo-side:

- `node --test tests-node/public-sync-host-checklist-contract.test.js`
  - `pass`
- `node --test tests-node/prod-ops-public-sync-contract.test.js`
  - `fail`, pero por una deuda previa fuera del write set de esta slice:
    el test espera el snippet `$authRecommendedMode = 'google_oauth'` dentro de
    `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1`, archivo que no fue modificado
    aqui.

## Regresion posterior al publish

Despues de publicar `2ba6ad5e`, la telemetria host-side dejo de refrescarse y
quedo clavada en el commit anterior
`798d6b4ea725e8e3f965000064a1512b60c9f656`.

Snapshot rojo vigente revalidado el `2026-03-26` a las `05:05:38Z`:

- `node agent-orchestrator.js jobs verify public_main_sync --json`
    - `healthy=false`
    - `operationally_healthy=false`
    - `failureReason=stale`
    - `age_seconds=395`
    - `checked_at=2026-03-26T04:58:29.2426758+00:00`
    - `last_success_at=2026-03-25T23:58:25.0326878-05:00`
    - `deployedCommit=798d6b4ea725e8e3f965000064a1512b60c9f656`
    - `currentHead=remoteHead=798d6b4ea725e8e3f965000064a1512b60c9f656`
    - `headDrift=false`
    - `telemetryGap=false`
    - `dirtyPathsCount=0`
    - `jobId=8d31e299-7e57-4959-80b5-aaa2d73e9674`
- `node agent-orchestrator.js status --json`
    - `job:public_main_sync=red(stale)`
    - `runtime:operator_auth=green`
    - `required_checks_ok=false`
    - `release_ready=false`

Interpretacion operativa: el repo ya quedo endurecido para la ruta Windows
real, pero el incidente no esta resuelto. El bloqueo vigente sigue siendo
host-side: `lastCheckedAt` no vuelve a moverse y el host sigue reportando el
commit previo al hardening publicado en `main`.

## Estado vigente

`CDX-009` vuelve a `blocked`.

La slice deja valor real en repo:

1. el triage futuro ya arranca por `windows_selfhosted`;
2. la evidencia deja congelada la ventana verde util y la regresion roja
   posterior; y
3. el bloqueo restante queda claramente aislado en el scheduler/runner
   host-side, no en el contrato publico de `health` ni en
   `runtime:operator_auth`.
