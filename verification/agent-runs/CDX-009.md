# CDX-009 - Public sync incident recovery and Windows/self-hosted hardening

## Objetivo

Recuperar `public_main_sync`, confirmar el host productivo real y endurecer el
triage para `windows_selfhosted` sin cambiar el contrato publico de `health`.

## Ventana verde observada

Ventana revalidada el `2026-03-26` entre `04:53:38Z` y `04:54:32Z`.

- `node agent-orchestrator.js jobs verify public_main_sync --json`
    - `verification_source=health_url`
    - `verified=true`
    - `healthy=true`
    - `operationally_healthy=true`
    - `state=idle`
    - `age_seconds=54`
    - `checked_at=2026-03-26T04:53:38.7896399+00:00`
    - `last_success_at=2026-03-25T23:53:34.6219328-05:00`
    - `currentHead=remoteHead=798d6b4ea725e8e3f965000064a1512b60c9f656`
    - `headDrift=false`
    - `telemetryGap=false`
    - `dirtyPathsCount=0`
    - `failureReason=""`
    - `jobId=8d31e299-7e57-4959-80b5-aaa2d73e9674`
- Telemetria viva del target actual
    - `repo_path=C:\dev\pielarmonia-clean-main`
    - `status_path=C:\ProgramData\Pielarmonia\hosting\public-sync-status.json`
    - `log_path=C:\ProgramData\Pielarmonia\hosting\main-sync.runtime.log`
    - `lock_file=C:\tmp\sync-pielarmonia.lock`
- `node agent-orchestrator.js status --json`
    - `policy.revision=754`
    - `job:public_main_sync=green`
    - `runtime:operator_auth=green`
    - `required_checks_ok=true`
    - `release_ready=true`
- `node agent-orchestrator.js runtime verify google_oauth --json`
    - `runtime:operator_auth=green`
    - `figo_queue=healthy`
    - `leadops_worker=degraded(worker_disabled)` sigue fuera de scope

La ventana verde confirmo dos cosas utiles: la ruta productiva real hoy es
`windows_selfhosted`, y el hardening repo-side no rompe el contrato publico de
`checks.publicSync`.

## Estado revalidado actual

Revalidado nuevamente el `2026-03-26` alrededor de `11:42Z` a `11:48Z`.

- `https://pielarmonia.com/__hosting/runtime`
    - `current_commit=a35ee6833c18b657b1b4d5bdaf9d21e3b3c00325`
    - `desired_commit=a35ee6833c18b657b1b4d5bdaf9d21e3b3c00325`
    - `status_source=hosting_runtime_fingerprint`
- `https://pielarmonia.com/admin-auth.php?action=status`
    - HTTP `200`
    - `mode=google_oauth`
    - `transport=web_broker`
    - `configured=true`
- `node agent-orchestrator.js jobs verify public_main_sync --json`
    - `healthy=false`
    - `operationally_healthy=false`
    - `failureReason=stale`
    - `age_seconds=459`
    - `checked_at=2026-03-26T11:42:20.7901565+00:00`
    - `last_success_at=2026-03-26T06:42:16.6361220-05:00`
    - `deployedCommit=currentHead=remoteHead=a35ee6833c18b657b1b4d5bdaf9d21e3b3c00325`
    - `headDrift=false`
    - `telemetryGap=false`
    - `dirtyPathsCount=0`
- `node agent-orchestrator.js runtime verify google_oauth --json`
    - `required_checks.job:public_main_sync=red(stale)`
    - `required_checks.runtime:operator_auth=green`
    - `figo_queue=healthy`
    - `leadops_worker=degraded(worker_disabled)` sigue fuera de scope
    - `operator_auth=healthy`

Interpretacion operativa actual:

- el host productivo ya sirve el commit correcto `a35ee683`;
- `operator_auth` se mantuvo sano durante la recaida;
- el incidente ya no es drift de deploy ni mismatch de commit;
- el bloqueo vigente es que la telemetria del cron `public_main_sync` dejo de
  refrescarse otra vez y el check vuelve a `stale`.

## Hardening repo-side publicado

El hardening operativo quedo publicado en `main` con
`2ba6ad5e681a127d5c70a84ff2cbb35c3994e11c`
(`chore: harden public main sync windows recovery`).

Cambios aplicados:

- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`
    - promueve `jobs verify public_main_sync --json` como discovery obligatorio;
    - declara `windows_selfhosted` como target primario actual; y
    - deja `linux_legacy` como fallback explicito.
- `scripts/ops/prod/CHECKLIST-HOST-PUBLIC-SYNC.ps1`
    - agrega `-HostProfile windows_selfhosted|linux_legacy`;
    - usa `windows_selfhosted` como default efectivo con
      `RepoPath=C:\dev\pielarmonia-clean-main`,
      `StatusPath=C:\ProgramData\Pielarmonia\hosting\public-sync-status.json`,
      `LogPath=C:\ProgramData\Pielarmonia\hosting\main-sync.runtime.log` y
      `LockPath=C:\tmp\sync-pielarmonia.lock`; y
    - preserva los comandos canonicos Linux bajo `linux_legacy`.

Validacion repo-side:

- `node --test tests-node/public-sync-host-checklist-contract.test.js`
    - `pass`
- `node --test tests-node/prod-ops-public-sync-contract.test.js`
    - `fail`, pero por una deuda previa fuera del write set de esta slice:
      el test espera el snippet `$authRecommendedMode = 'google_oauth'` dentro de
      `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1`, archivo que no fue modificado
      aqui.

## Regresion recurrente despues de la recuperacion

La deriva observada mas tarde ya no apunta al commit viejo del host sino a una
recaida del scheduler/telemetria:

- `deployedCommit`, `currentHead` y `remoteHead` quedan alineados en
  `a35ee683`;
- `__hosting/runtime` confirma `current_commit=desired_commit`;
- aun asi `checks.publicSync` vuelve a `stale` por `lastCheckedAt` congelado.

Eso reduce el problema restante a la ejecucion/telemetria del cron
`public_main_sync`, no al rollout del release ni al contrato publico de auth.

## Estado vigente

`CDX-009` sigue `blocked`.

La slice deja valor real en repo:

1. el triage futuro ya arranca por `windows_selfhosted`;
2. la evidencia deja congelada una ventana verde real seguida por una recaida
   `stale` con el commit correcto ya desplegado; y
3. el bloqueo restante queda claramente aislado en la telemetria/ejecucion del
   scheduler host-side, no en el contrato publico de `health` ni en
   `runtime:operator_auth`.

## Revalidacion adicional 2026-03-26 16:46Z

Se revalido el incidente nuevamente desde `main` ya normalizado y con runtime
local disponible.

### Hallazgo remoto actual

Las rutas publicas siguen cayendo antes de llegar al contrato PHP:

- `https://pielarmonia.com/api.php?resource=health`
  - HTTP `502`
  - body `error code: 502`
- `https://pielarmonia.com/api.php?resource=health-diagnostics`
  - HTTP `502`
  - body `error code: 502`
- `https://pielarmonia.com/api.php?resource=operator-auth-status`
  - HTTP `502`
- `https://pielarmonia.com/admin-auth.php?action=status`
  - HTTP `502`
- `node bin/admin-openclaw-rollout-diagnostic.js --domain https://pielarmonia.com --json`
  - `diagnosis=operator_auth_surface_unreachable`
  - `next_action=Revisar conectividad o routing de api.php?resource=operator-auth-status en el dominio remoto.`

La evidencia ya no apunta a un mismatch del contrato `operator-auth` publicado,
sino a un fallo de edge/origen que ocurre antes de que `api.php` o
`admin-auth.php` devuelvan JSON.

### Correccion repo-side aplicada

Se corrigio una regresion local en [`lib/auth.php`](../../lib/auth.php):

- `OPERATOR_AUTH_SOURCE` vuelve a tratar `openclaw_chatgpt` como modo primario;
- `operator_auth_normalize_mode()` preserva `openclaw_chatgpt` como contrato
  publico y solo usa `google_oauth` como alias/backend compatible.

Esto evita que la fachada admin y los snapshots de readiness expongan
`mode=google_oauth` cuando el entorno realmente esta configurado para
`openclaw_chatgpt`.

### Validacion local del fix

- `php tests/test_operator_auth.php`
  - `6 passed, 0 failed`
- `php tests/test_admin_auth_openclaw_facade.php`
  - `9 passed, 0 failed`
- `node --test tests-node/openclaw-auth-rollout-diagnostic.test.js tests-node/prod-openclaw-auth-rollout-contract.test.js`
  - `11 passed, 0 failed`
- Smoke directo del health local con `PIELARMONIA_OPERATOR_AUTH_MODE=openclaw_chatgpt`
  - responde `200`
  - conserva `checks.publicSync`
  - ya no rompe el contrato local de readiness/auth

### Lectura operativa actualizada

Quedan dos hechos simultaneos:

1. Habia una regresion repo-side menor en el contrato OpenClaw (`mode`/`recommendedMode`) y ya quedo corregida localmente.
2. El incidente que bloquea `CDX-009` sigue siendo host-side: el dominio
   publico devuelve `502` incluso para `health-diagnostics`, asi que hoy el
   bloqueo real esta en routing/origen/Cloudflare del host productivo, no en el
   codigo PHP revalidado localmente.

## Revalidacion operativa 2026-03-26 16:48Z

Se intento el rescue por GitHub Actions con el token ya persistido localmente,
sin volver a depender de credenciales SSH locales.

### Billing y token

- El bloqueo inicial de GitHub Actions por billing ya no aplica.
- El workflow manual vuelve a arrancar jobs en `ubuntu-latest`, asi que el PAT
  y la cuenta ya no son el limitante actual.

### Workflow SSH Windows

- `repair-windows-hosting-over-ssh.yml`
  - run `23606371475`
  - llega a `Validate SSH inputs`
  - falla con `Falta SSH_PASSWORD para repair Windows over SSH.`
- Estado de secrets/vars del repo confirmado por API:
  - `SSH_HOST`: presente
  - `SSH_USERNAME`: presente
  - `SSH_PASSWORD`: ausente

### Workflow self-hosted Windows

- `promote-windows-hosting-target.yml`
  - run `23606497249`
  - queda `queued`
- API de runners del repo:
  - `total_count=0`
  - no hay runner `self-hosted, Windows` disponible para tomar el job

### Diagnostico de conectividad GitHub-hosted

- `diagnose-host-connectivity.yml`
  - run `23606738147`
  - conclusion `success`
  - artifact `connectivity-report.json`
- Resultado canonico:
  - `status=unreachable`
  - `configured_host_count=2`
  - `open_targets=[]`
- El host configurado en `secrets.SSH_HOST` y `secrets.FTP_SERVER` no expone
  ningun puerto abierto desde GitHub-hosted dentro de la lista probada:
  `21,22,443,990,2121,2222,65002,21098,22022,22222,10022,7822`
- El workflow abrio/actualizo el incidente operativo
  `#461 [ALERTA PROD] Diagnose host connectivity sin ruta de deploy`

### Contraste desde esta maquina

- Probe TCP manual al mismo host/puertos desde esta shell:
  - todos los intentos terminan en `timeout`
  - no hay diferencia util entre GitHub-hosted y este origen local

### Estado final de la slice

- `node agent-orchestrator.js jobs verify public_main_sync --json`
  - `checked_at=2026-03-26T16:47:30.429Z`
  - `failure_reason=health_http_502`
  - `repo_path=C:\dev\pielarmonia-clean-main`
  - `head_drift=false`
  - `telemetry_gap=false`
- El bloqueo ya no es ambiguo:
  1. billing: resuelto
  2. PAT GitHub: operativo
  3. rescue SSH Windows: bloqueado por `SSH_PASSWORD` ausente
  4. fallback self-hosted: bloqueado por ausencia total de runners Windows
  5. host configurado: sin puertos alcanzables desde GitHub-hosted ni desde esta maquina

Con la evidencia actual, `CDX-009` queda bloqueado por infraestructura/remoto
fuera del repo. No hay una tercera via automatica viable desde esta shell
mientras el host siga inaccesible o no reaparezca un acceso operativo real
(`SSH_PASSWORD`, runner Windows vivo o restauracion de conectividad al host).

## Checkpoint canonico de reencuadre 2026-03-26 16:50Z

`CDX-009` queda reencuadrado como blocker externo reconocido del foco
`Admin operativo`, no como recovery repo-side ejecutable desde esta maquina.

### Hechos operativos canonicos

1. `repair-windows-hosting-over-ssh.yml`
   - run `23606371475`
   - falla por `Falta SSH_PASSWORD para repair Windows over SSH.`
2. `promote-windows-hosting-target.yml`
   - run `23606497249`
   - no corre porque el repo tiene `0` runners `self-hosted, Windows`
3. `diagnose-host-connectivity.yml`
   - run `23606738147`
   - `status=unreachable`
   - `open_targets=[]`
   - host configurado: `101.47.4.223`

### Referencia canonica de seguimiento

- incidente operativo: `issue#461`
- checks bloqueantes vigentes:
  - `job:public_main_sync=red(health_http_502)`
  - `runtime:operator_auth=red(auth_status_http_502)`

### Criterio de reanudacion

La reanudacion del frente host-side solo tiene sentido si reaparece al menos
una via operativa real:

- `SSH_PASSWORD` cargado y valido para el workflow SSH;
- runner `self-hosted, Windows` nuevamente activo; o
- conectividad recuperada al host `101.47.4.223` desde origenes operativos.
