# CDX-056

## Resumen

- Slice frontend canónica del frente
  `STRAT-2026-03-turnero-web-pilot-local-first`.
- Reemplaza la slice local `CDX-053` y reaterriza en el canónico el replay de
  surfaces web y runtime del piloto `web_pilot`.
- El objetivo es que admin, operador, kiosco y sala lean el perfil clínico
  `web_pilot` como salida local válida, sin desktop/Android como blockers.

## Write Set

- `content/turnero/clinic-profile.json`
- `content/turnero/clinic-profiles/piel-armonia-quito.json`
- `src/apps/admin-v3/shared/modules/queue/render/section/install-hub/pilot/model.js`
- `admin.js`
- `js/queue-operator.js`
- `tests/admin-queue.spec.js`

## Cambios aplicados

- El perfil activo y el catalogado de `Piel Armonia Quito` pasan a
  `release.mode=web_pilot`, con `admin_mode_default=basic`,
  `separate_deploy=true` y `native_apps_blocking=false`.
- El install hub del admin deja de tratar desktop/Android como blockers del
  corte local-first y los presenta como carriles posteriores.
- Se regeneran los artefactos publicados del runtime desde fuente
  (`admin.js`, `js/queue-operator.js`) en lugar de resolver por merge manual.
- La suite Playwright de `admin-queue` queda alineada al contrato
  `web_pilot`, incluyendo heartbeats locales, rutas canónicas y el hub
  operativo del piloto.

## Validacion

```bash
node bin/turnero-clinic-profile.js validate --id piel-armonia-quito --json
node bin/turnero-clinic-profile.js stage --id piel-armonia-quito --json
npm run build:turnero:runtime
npm run check:turnero:runtime
node bin/run-turnero-web-pilot-ui.js
```

Resultado:

- `validate` y `stage` cerraron `ok=true` para `piel-armonia-quito`.
- `build:turnero:runtime` y `check:turnero:runtime` quedaron en verde.
- `node bin/run-turnero-web-pilot-ui.js` termino `ok=true` con reporte en
  `verification/turnero-web-pilot/ui-report.json`.
- La corrida UI cerro con:
  - `Admin queue`: `PASS (206734ms)`
  - `Queue kiosk`: `PASS (27392ms)`
  - `Queue operator`: `PASS (31305ms)`
  - `Queue display`: `PASS (14477ms)`
  - `Queue integrated flow`: `PASS (45975ms)`

## Notas

- `suite_v2` se preserva como carril legado soportado; esta slice solo cambia
  la salida canónica del piloto local por clínica.
- Esta evidencia reemplaza la trazabilidad local de `CDX-053`.

## Required Checks

- required_check: test:turnero:web-pilot:contracts | state: green | command: npm run test:turnero:web-pilot:contracts
- required_check: test:turnero:web-pilot:php-contract | state: green | command: npm run test:turnero:web-pilot:php-contract
- required_check: test:turnero:web-pilot:ui | state: green | command: npm run test:turnero:web-pilot:ui
*** Add File: /Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm/verification/agent-runs/CDX-057.md
# CDX-057

## Resumen

- Slice backend/gates canónica del frente
  `STRAT-2026-03-turnero-web-pilot-local-first`.
- Reemplaza la slice local `CDX-054` y consolida contrato, readiness, scripts y
  runner local del carril `web_pilot`.
- El objetivo es validar el piloto web local por clínica sin arrastrar
  `verify-remote`, `smoke:prod:*` ni `public_main_sync`.

## Write Set

- `controllers/AdminDataController.php`
- `package.json`
- `bin/run-playwright-local.js`
- `tests-node/admin-data-turnero-clinic-profile-contract.test.js`
- `tests-node/admin-queue-pilot-readiness.test.js`
- `tests-node/health-turnero-pilot-contract.test.js`
- `tests-node/queue-operator-shell-state.test.js`
- `tests-node/turnero-clinic-profile-registry.test.js`
- `tests-node/turnero-runtime-contract.test.js`
- `tests-node/turnero-v2-gate-contract.test.js`

## Cambios aplicados

- `AdminDataController` acepta `release.mode=web_pilot` como salida local
  válida y remapea operador, kiosco y sala al carril browser cuando el piloto
  local no exige shells nativas.
- El readiness del admin y del hardware deja desktop/Android como diferidos
  cuando la clínica está en `web_pilot`.
- `package.json` promueve `gate:turnero -> gate:turnero:web-pilot` y hace que
  `test:turnero:web-pilot:ui` use el wrapper canónico
  `bin/run-turnero-web-pilot-ui.js`.
- `bin/run-playwright-local.js` deja `stdout/stderr` del servidor PHP local en
  `ignore`, evitando el stall por buffers llenos bajo carga Playwright.
- La batería Node cubre explícitamente `web_pilot` con nativas no bloqueantes y
  mantiene `suite_v2` como carril legado soportado.

## Validacion

```bash
node bin/turnero-clinic-profile.js validate --id piel-armonia-quito --json
node bin/turnero-clinic-profile.js stage --id piel-armonia-quito --json
npm run test:turnero:web-pilot:contracts
npm run test:turnero:web-pilot:php-contract
node --test tests-node/run-playwright-local.test.js tests-node/turnero-web-pilot-gate-contract.test.js
npm run gate:turnero
```

Resultado:

- `validate` y `stage` cerraron `ok=true` para `piel-armonia-quito`.
- `test:turnero:web-pilot:contracts`: `186/186` verdes.
- `test:turnero:web-pilot:php-contract`: verde
  (`Figo queue core tests passed`, `All queue service tests passed`).
- `run-playwright-local.test.js` y `turnero-web-pilot-gate-contract.test.js`:
  `9/9` verdes.
- `gate:turnero`: verde end-to-end, incluyendo contratos, PHP, build/check del
  runtime y la suite UI completa del piloto web.

## Notas

- El fix del runner local queda dentro de scope porque estabiliza el gate local
  del piloto `web_pilot`; no toca deploy, host productivo ni runbooks remotos.
- Esta evidencia reemplaza la trazabilidad local de `CDX-054`.
*** Add File: /Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm/verification/agent-runs/CDX-058.md
# CDX-058

## Resumen

- Slice transversal de integración canónica y closeout del frente
  `STRAT-2026-03-turnero-web-pilot-local-first`.
- Reemplaza la slice local `CDX-055`.
- Su responsabilidad es remapear la ola local al canónico con IDs nuevos
  (`CDX-056` frontend, `CDX-057` backend, `CDX-058` gobernanza), dejar
  `CDX-009` como deuda externa no bloqueante y preparar el publish/closeout
  final sobre `origin/main`.

## Write Set

- `AGENT_BOARD.yaml`
- `PLAN_MAESTRO_CODEX_2026.md`
- `tools/agent-orchestrator/domain/focus.js`
- `tools/agent-orchestrator/domain/strategy.js`
- `verification/agent-runs/CDX-056.md`
- `verification/agent-runs/CDX-057.md`
- `verification/agent-runs/CDX-058.md`

## Cambios aplicados

- `strategy.active` queda en
  `STRAT-2026-03-turnero-web-pilot-local-first`.
- `CDX-009` deja de consumir slot del frente activo y queda como deuda externa
  `backlog` no bloqueante para el piloto web local-first.
- La numeración canónica se corrige para seguir el remap acordado:
  - `CDX-056`: frontend replay, reemplaza `local:CDX-053`
  - `CDX-057`: backend/gates, reemplaza `local:CDX-054`
  - `CDX-058`: gobernanza y closeout, reemplaza `local:CDX-055`
- `focus.js` y `strategy.js` quedan alineados para soportar el seed activo del
  frente `turnero-web-pilot-local-first`.
- El plan maestro deja de presentar `CDX-056` como rescue backend heredado y
  reencuadra esas notas como historial preintegración.

## Validacion

Pendiente de completar con los checks de gobernanza del closeout:

```bash
node agent-orchestrator.js focus verify --json
node agent-orchestrator.js status --json
node agent-orchestrator.js task ls --active --json
node agent-orchestrator.js codex-check --json
node agent-orchestrator.js board doctor --json --profile ci
node agent-orchestrator.js handoffs lint --json
```

## Notas

- `public_main_sync` sigue explícitamente fuera de alcance y solo se tolera
  como warning externo.
- El publish final de esta slice debe salir por el closeout canónico de
  `CDX-058`, no por publicación manual excepcional.
