# AG-244 - Pilot readiness evidence: verify public_main_sync and host sync path

Fecha: 2026-03-14
Owner: codex_backend_ops
Dominio: readiness

## Objetivo

Dejar evidencia operativa accionable para el paso `pilot_readiness_evidence`
del foco `FOCUS-2026-03-admin-operativo-cut-1`, aclarando como leer el estado
actual de `public_main_sync` cuando el orquestador responde
`verification_source=registry_only` con `failure_reason=unverified`.

## Cambio aplicado

- Se actualizo [docs/AGENT_ORCHESTRATION_RUNBOOK.md](/home/deck/Documents/GitHub/piel-en-armonia/docs/AGENT_ORCHESTRATION_RUNBOOK.md) para que la operacion diaria y el escalamiento de `jobs verify public_main_sync --json` distingan explicitamente el caso `registry_only/unverified` de un `headDrift` o `working_tree_dirty`.
- Se actualizo [docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md](/home/deck/Documents/GitHub/piel-en-armonia/docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md) con una regla canonica de triage: si el verificador solo ve el registro y no la telemetria viva del host, primero se confirma `health`, `health-diagnostics`, el status file y el wrapper canonico antes de clasificar el incidente como drift del repo.
- Se reforzo [scripts/ops/prod/MONITOR-PRODUCCION.ps1](/home/deck/Documents/GitHub/piel-en-armonia/scripts/ops/prod/MONITOR-PRODUCCION.ps1) con una advertencia explicita para el patron `state=unknown + failureReason=unverified + heads vacios + dirtyPathsCount=0`, enlazandolo con la lectura `verification_source=registry_only` del orquestador.

## Estado observado

`node agent-orchestrator.js jobs verify public_main_sync --json` sigue
devolviendo el estado que motivó esta evidencia:

- `verification_source=registry_only`
- `failure_reason=unverified`
- `state=unknown`
- `current_head=""`
- `remote_head=""`
- `dirty_paths_count=0`

Interpretacion operativa:

- el orquestador puede confirmar que el job existe y esta configurado;
- no puede todavía verificar telemetria viva del host;
- por eso el primer paso correcto es recuperar evidencia de `health`,
  `health-diagnostics` y `/var/lib/pielarmonia/public-sync-status.json`,
  no limpiar el repo del VPS a ciegas.

## Validacion ejecutada

1. `node agent-orchestrator.js jobs verify public_main_sync --json`
2. `node agent-orchestrator.js focus status --json`
3. revision manual de los runbooks y del monitor para confirmar que el mismo caso operativo queda descrito de forma consistente

## Limitaciones

- No se pudo ejecutar un parseo de PowerShell en este entorno porque `pwsh`
  no esta instalado.
- Esta tarea no recupera la salud de `public_main_sync`; deja el diagnostico y
  el camino de triage correctamente explicitados para el foco.

## Resultado

- `AG-244` deja documentado el estado real del check requerido
  `job:public_main_sync` para el corte `admin operativo`.
- El foco sigue alineado con `2` tareas en `pilot_readiness_evidence` y ya no
  mezcla este rojo con drift de repo sin evidencia.
