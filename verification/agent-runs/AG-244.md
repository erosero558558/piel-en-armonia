# AG-244 — Reconstructed Evidence

Reconstructed on 2026-03-21 because the canonical evidence file was missing.

Task snapshot:
- Title: `Pilot readiness evidence: verify public_main_sync and host sync path`
- Files: `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`, `docs/AGENT_ORCHESTRATION_RUNBOOK.md`, `scripts/ops/prod/MONITOR-PRODUCCION.ps1`

Current reproducible checks:
- `node --test tests-node/agent-jobs-cli.test.js` passed on 2026-03-21.
- `node agent-orchestrator.js jobs verify public_main_sync --json` currently returns `ok=false`, `verification_source=registry_only`, `failure_reason=unverified`.

Current live note:
- The repo-side contracts for `public_main_sync` are green locally, but the public host is still not exposing verifiable telemetry through `health_url`.

Notes:
- This rebuild documents the current runnable evidence and the still-open host-side gap.
