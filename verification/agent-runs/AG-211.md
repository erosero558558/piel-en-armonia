# AG-211 - Dispatch connectivity diagnosis from deploy-hosting transport preflight

## Summary

- Extended `deploy-hosting.yml` so a transport preflight failure now dispatches `diagnose-host-connectivity.yml` automatically from the same run.
- Propagated the downstream diagnose run status and URL into the transport incident and the deploy workflow summary.
- Updated the public main update runbook to document the new automatic diagnose handoff from deploy-hosting.

## Files

- `.github/workflows/deploy-hosting.yml`
- `tests-node/deploy-hosting-workflow-contract.test.js`
- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`

## Verification

- `node --test tests-node/deploy-hosting-workflow-contract.test.js`

## Result

- Contract suite passed: `11/11`.
- `deploy-hosting.yml` now dispatches `diagnose-host-connectivity.yml` when `transport_preflight_reason=runner_tcp_unreachable` and records `connectivity_diagnose_run_status` plus `connectivity_diagnose_run_url`.
- The transport incident `[ALERTA PROD] Deploy Hosting transporte bloqueado desde GitHub Runner` now carries the diagnose run context so the first failure signal already links to the deeper network drill-down.
