# S15-13 — BLOCKERS.md sincronizado con stuck.js

## Resumen

- Se agregó `syncBlockersMarkdown()` en `bin/stuck.js` para centralizar la sincronización de `BLOCKERS.md`.
- `node bin/stuck.js list` ahora reescribe `BLOCKERS.md` desde `data/claims/stuck.json` antes de mostrar blockers activos.
- `clear` y `markStuck` reutilizan el mismo helper para mantener un único camino de sincronización.
- Se añadió cobertura en `tests-node/stuck-cli.test.js` para el caso de drift: `BLOCKERS.md` stale + `stuck.json` vigente.

## Validación

- `node --check bin/stuck.js`
- `node --test tests-node/stuck-cli.test.js`
- `git diff --check`

## Resultado

- `list` ya corrige `BLOCKERS.md` si estaba desalineado.
- `clear` sigue retirando blockers resueltos del markdown.
- Suite puntual: `4 passed`.
