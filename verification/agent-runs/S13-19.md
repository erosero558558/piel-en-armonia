# S13-19 — Loading state visible en `es/agendar/`

## Resumen

Se validó el loading state visible del booking público en `es/agendar/`, incluyendo:

- overlay semitransparente sobre el formulario
- spinner inline en el botón activo
- mensaje visible `"Confirmando tu cita..."`
- uso del componente `.skeleton` de `styles/aurora-public.css`

Además se endureció la spec `tests/agendar-loading-state.spec.js` para que siga la UI publicada actual del booking y no labels obsoletos.

## Evidencia técnica

Archivos relevantes ya presentes en `main`:

- `es/agendar/index.html`
- `es/agendar/agendar.css`
- `es/agendar/agendar.js`
- `styles/aurora-public.css`
- `tests/agendar-loading-state.spec.js`

## Validación

Servidor de prueba:

- `http://127.0.0.1:8013` con `bin/local-stage-router.php`

Comandos ejecutados:

```bash
node --check es/agendar/agendar.js
node --check tests/agendar-loading-state.spec.js
TEST_BASE_URL=http://127.0.0.1:8013 npx playwright test tests/agendar-loading-state.spec.js --workers=1
```

Resultado:

- `3 passed (14.4s)`

Cobertura validada:

1. Reserva normal
2. Evaluación de telemedicina
3. Lista de espera
