# AG-025 - Post-Deploy Gate hardening y evidencia local

## Goal
Mejorar diagnostico del workflow `post-deploy-gate` y verificar que el gate local vuelve a verde.

## Implementado
- `.github/workflows/post-deploy-gate.yml`
  - Issue de alerta con mas contexto operativo (`gate_outcome`, `change_class`, wait efectivo, bench runs efectivos, modo calendario)
  - Label `production-alert`
- `VERIFICAR-DESPLIEGUE.ps1`
  - Ajuste advisory (ver AG-024) para evitar falso rojo en escenarios backend-only con drift frontend remoto

## Validacion local (2026-02-25)
- `powershell -ExecutionPolicy Bypass -File .\\GATE-POSTDEPLOY.ps1` -> **OK**
- Resultado:
  - `Verificacion de despliegue` -> OK
  - `Smoke Produccion` -> `19/19 checks OK`
  - `Bench Produccion API` -> OK
  - p95 observados:
    - `health`: 362.95 ms
    - `reviews`: 376.36 ms
    - `availability`: 369.56 ms
    - `figo-get`: 428.85 ms
    - `figo-post`: 407.16 ms

## Parse/consistencia local
- `.github/workflows/post-deploy-gate.yml` -> YAML OK

## Validacion remota (rama de prueba)
- Run `22384295980` (manual, branch `codex/prod-readiness-workflows-20260225`) ejecuta el workflow completo y publica reporte.
- Resultado: `failure` en `Ejecutar gate estricto`, **esperado** para rama no desplegada:
  - `deploy freshness` compara produccion contra `HEAD` de la rama (incluye cambios runtime como `admin.js`)
  - `index remoto styles.css diferente` por drift real entre produccion y rama
- Conclusion: el workflow/diagnostico corre en remoto; el verde final requiere merge+deploy y rerun sobre `main`.

## Estado en main (observado)
- `Post-Deploy Gate (Git Sync)` en `main`: run `22384855384` -> **success**
- Paso `Ejecutar gate estricto` -> **success**
- Paso `Cerrar incidente de gate al recuperar` -> **success**
- Issue `#281` (`[ALERTA PROD] Gate post-deploy fallando`) -> **CLOSED**

## Validacion post-merge adicional (PR #285)
- Merge a `main`: commit `2374f971f7e5849617c921734bb3731dab8c8147`
- Run `Post-Deploy Gate (Git Sync)` en `main`: `22386030764` (`push`) -> **failure**
  - Causa observada via artifact `last-deploy-verify.json`: drift temporal de deploy
    - mismatch `index-ref:styles.css`
    - `LocalHash`: `styles.css?v=inline-20260224-extracted1`
    - `RemoteHash`: `styles.css?v=figo-20260222-slotservicefix1`
  - Se abrio alerta automatica `#286` (`[ALERTA PROD] Gate post-deploy fallando`)
- Rerun manual en `main`: `22386245922` (`workflow_dispatch`) -> **success**
  - `Ejecutar gate estricto` -> **success**
  - Contratos calendario/Figo -> **success**
  - Alerta `#286` -> **CLOSED** (autocierre con comentario de recovery)
- Verificacion adicional:
  - `gh issue list --state open --search "[ALERTA PROD]"` (2026-02-25) -> sin alertas abiertas

## Estado final
- P0 `post-deploy-gate` recuperado en `main` con evidencia de falso rojo temporal + rerun exitoso.
- Sin pendientes externos para `AG-025` (solo monitoreo normal).
