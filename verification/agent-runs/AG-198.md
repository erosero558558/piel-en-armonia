# AG-198 - Public main sync dirty-tree self-heal hardening

- Date: `2026-03-11`
- Task title: `Public main sync dirty-tree self-heal hardening`

## Outcome

- `bin/deploy-public-v3-live.sh` now resets the full tracked Composer-generated metadata surface that can leave `/var/www/figo` dirty after dependency installs, including `vendor/autoload.php`, `vendor/bin/*`, and the tracked `vendor/composer/*` autoload manifests.
- `bin/deploy-public-v3-cron-sync.sh` now limits self-heal to canonical generated public/runtime outputs plus Composer-generated vendor metadata, and writes dirty-path telemetry (`dirty_paths_count`, `dirty_paths_sample`, `dirty_paths`) into the runtime status JSON when the host still blocks on `working_tree_dirty`.
- The deploy/cron script contracts now cover the expanded Composer allowlist and the new dirty-path telemetry so the self-heal behavior stays locked in.

## Verification

- `node --test tests-node/deploy-public-v3-cron-sync-script.test.js tests-node/deploy-public-v3-live-script.test.js` -> `PASS` (`12/12`)
- `node agent-orchestrator.js jobs verify public_main_sync --json` -> still `healthy=false` on `2026-03-11`, `state=failed`, `last_error_message=working_tree_dirty`

## Operational Note

- The remaining `public_main_sync_failed` warning reflects the live host state before this hardening is deployed.
- Once these changes reach `main`, the next cron run should either self-heal the Composer/generated drift or expose the exact residual dirty paths in the status payload for follow-up.
