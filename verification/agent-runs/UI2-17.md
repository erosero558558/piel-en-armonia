# UI2-17 — Consistency pass de formularios

## Alcance ejecutado

- Unifique clases del design system (`.input`, `.select`, `.textarea`, `.btn-primary`) y wiring accesible en los formularios vivos de `pre-consulta` y admin.
- Alinee las fuentes canonicas de booking (`BookingShellV3.astro`, `BookingShellV5.astro` y `src/apps/booking/modules/booking-form.js`) para que mantengan mensajes inline estables y estados `error/success`.
- Extendi verificaciones en `bin/lib/gate-checks.js`, `bin/verify.js`, `tests/ui2-form-consistency.spec.js` y `tests-node/ui2-form-consistency-contract.test.js`.
- Ajuste `bin/gate.js` para aceptar tareas `UI2-*` ademas de `UI-*`.

## Nota de snapshot

- En `origin/main` al tomar la tarea no existian rutas publicas Astro vivas que expusieran `#v5-booking` o `#appointmentForm`; por eso la cobertura visual quedo en `pre-consulta` y admin, mientras booking se cubrio por contrato de fuente y rebuild del runtime.
- Tampoco habia formularios portal activos bajo `es/portal/**` en este snapshot; no se mezclo una implementacion nueva fuera del scope de consistencia.

## Validacion

- `npm run build:public:v6`
- `npm run build:turnero:runtime`
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/ui2-form-consistency.spec.js --workers=1`
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/pre-consulta-public-intake.spec.js --workers=1`
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/admin-clinical-history-workspace.spec.js -g "exporta la HCE completa" --workers=1`
- `node --test tests-node/ui2-form-consistency-contract.test.js`
- `node --test --test-name-pattern "UI2-17" tests-node/gate-task-checks.test.js`
- `git diff --check`
- `node bin/gate.js UI2-17`

## Resultado

- Gate `UI2-17` en verde.
- `pre-consulta` y admin verificados visualmente con Playwright.
- Booking queda alineado a nivel de fuente y runtime con mensajes inline y estados accesibles estables.
