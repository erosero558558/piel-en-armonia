# S15-02 — Stuck task-id regex regression guard

## Summary

- Promoted the stuck task-id regex into an explicit `TASK_ID_PATTERN` contract inside `bin/stuck.js`.
- Added `isValidTaskId()` so the accepted `S*` and `UI*` task families are testable without invoking the CLI against the real repo.
- Extended `tests-node/stuck-cli.test.js` with coverage for `UI2-07`, ensuring the command no longer falls back to `Usage:` for UI tasks.

## Validation

- `node --check bin/stuck.js`
- `node --test tests-node/stuck-cli.test.js`
- `git diff --check`

## Result

- `bin/stuck.js` now exposes the task-id validator as a reusable contract.
- The regression test proves `UI2-07` flows through the blocker path successfully in the temp-repo harness.
