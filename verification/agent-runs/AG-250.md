# AG-250

## Resumen

- Tarea: restaurar `public_main_sync` en `health` publico y dejar autoheal host-side clasificado.
- Worktree: `/home/deck/Documents/GitHub/piel-push-public-sync-restore-20260316`
- Commit publicado: `597c338750257f8b799a33304efc5f6b257591a9`
- `origin/main` confirmado en la misma revision.

## Cambios implementados

- `controllers/HealthController.php`
  - `health` publico ahora expone un resumen seguro de `checks.publicSync`.
- `tools/agent-orchestrator/domain/jobs.js`
  - `jobs verify public_main_sync --json` clasifica `health_missing_public_sync` como `health_url/stale`.
- `.github/workflows/deploy-hosting.yml`
  - clasificacion post-deploy de `publicSync` (`recovered`, `host_stale_health_contract`, `public_sync_runtime_failed`)
  - bloqueo explicito de post-deploy lanes y fail final cuando `publicSync` sigue mal
- `.github/workflows/repair-git-sync.yml`
  - clasificacion post-repair de `publicSync`
  - integracion de esa clasificacion al autoheal y al summary
- runbook y contratos de tests alineados con la nueva secuencia host-side.

## Validacion local

### Node contracts

Comando:

```bash
NODE_PATH=/home/deck/Documents/GitHub/piel-en-armonia/node_modules node --test tests-node/agent-jobs-cli.test.js tests-node/orchestrator/jobs-engine.test.js tests-node/prod-ops-public-sync-contract.test.js tests-node/public-sync-host-checklist-contract.test.js tests-node/prod-monitor-public-sync-contract.test.js tests-node/deploy-hosting-workflow-contract.test.js
```

Resultado:

- `52` tests
- `52` passed
- `0` failed

### PHP integration

Comando:

```bash
/home/deck/pielarmonia-openclaw-origin-main/vendor/bin/phpunit --no-configuration --bootstrap /home/deck/pielarmonia-openclaw-origin-main/vendor/autoload.php tests/Integration/HealthVisibilityTest.php
```

Resultado:

- `7` tests
- `7` passed
- `0` failed

## Publicacion

Comando:

```bash
git push origin HEAD:main
```

Resultado:

- `origin/main` actualizado de `e13b5967510aa59b9c8f34407595cc7e9b4cd4a6`
- a `597c338750257f8b799a33304efc5f6b257591a9`

## Verificacion live post-push

### `origin/main`

```bash
git ls-remote --heads origin main
```

Resultado:

```text
597c338750257f8b799a33304efc5f6b257591a9	refs/heads/main
```

### `jobs verify public_main_sync --json`

Comando:

```bash
node agent-orchestrator.js jobs verify public_main_sync --json
```

Resultado relevante:

```json
{
  "ok": false,
  "job": {
    "verification_source": "health_url",
    "verified": false,
    "state": "stale",
    "failure_reason": "health_missing_public_sync",
    "last_error_message": "health_missing_public_sync"
  }
}
```

### `health` publico

Comando:

```bash
curl -s https://pielarmonia.com/api.php?resource=health
```

Resultado:

```json
{"ok":true,"status":"ok","storageReady":true,"dataDirWritable":true,"timingMs":14,"version":"20260314072914","timestamp":"2026-03-16T22:59:50-05:00"}
```

### `health-diagnostics`

Comando:

```bash
curl -s -o /tmp/healthdiag.out -w "%{http_code}" https://pielarmonia.com/api.php?resource=health-diagnostics
```

Resultado:

- HTTP `403`
- body: `{"ok":false,"error":"No autorizado"}`

## Estado de cierre

- El cambio canónico quedó publicado en `origin/main`.
- La clasificacion local y los workflows ya distinguen correctamente `health_missing_public_sync`.
- El host live todavia no refleja `checks.publicSync` en `health` publico despues del push.
- En este entorno no hay `gh` disponible ni un alias SSH operativo a produccion, asi que no fue posible disparar `repair-git-sync` / `deploy-hosting` ni completar el repair host-side desde aqui.
- `AG-250` no debe considerarse cerrado hasta que el host sirva `checks.publicSync.jobId=8d31e299-7e57-4959-80b5-aaa2d73e9674` y `node agent-orchestrator.js jobs verify public_main_sync --json` pase con `ok=true`.
