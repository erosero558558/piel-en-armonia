# AG-021 - C1 exit gate en CI (staging)

## Goal
Automatizar salida C1 en CI con smoke + Playwright contract de Figo antes de merge.

## Implementado
- `.github/workflows/ci.yml`
  - Nuevo job `c1-exit-gate`
  - Ejecuta contrato Playwright de Figo contra staging
  - Smoke del endpoint de chat con fallback `'/api/chat' -> '/figo-chat.php'`
  - Gate de latencia (p95 Figo <= 3000ms)
  - Integracion con `paths-filter` para skip cuando no hay cambios relevantes
  - `build` depende de `c1-exit-gate`
- `tests/figo-chat-contract.spec.js`
  - Soporte para `TEST_FIGO_CHAT_ENDPOINT_PATH` (default `/figo-chat.php`)

## Validacion local
- Parse YAML:
  - `.github/workflows/ci.yml` -> OK
- JS syntax:
  - `node --check tests/figo-chat-contract.spec.js` -> OK
  - `node --check eslint.config.js` -> OK (config relacionada al gate CI)

## Validacion remota (completa)
- Historial parcial (rama de validacion)
  - Run `CI` manual: `22384721088`
  - `c1-exit-gate` quedo `skipped` por fallos previos en `unit-tests` / `e2e-tests`.
- Cierre en PR (rama `codex/prod-readiness-workflows-20260225`)
  - Run `CI`: `22385913073` (`pull_request`) -> **success**
  - Job `c1-exit-gate` -> **success**
  - Pasos clave en verde:
    - `Resolve effective C1 base URL`
    - `Run C1 Figo contract gate (staging)`
    - `Smoke chat endpoint on staging`
  - `build` tambien queda **success**, confirmando dependencia efectiva (`needs`) del gate C1.
- Cierre en `main`
  - Run `CI`: `22386030769` (`push` en `main`) -> **success**
  - Job `c1-exit-gate` -> **success**
  - Pasos clave en verde:
    - `Resolve effective C1 base URL`
    - `Run C1 Figo contract gate (staging)`
    - `Smoke chat endpoint on staging`

## Estado
- Gate C1 ejecutado end-to-end en remoto (PR y `main`).
- Criterio de aceptacion cumplido para `AG-021`.
