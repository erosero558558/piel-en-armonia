# AG-021 - C1 exit gate en CI (staging)

## Goal
Automatizar salida C1 en CI con smoke + Playwright contract de Figo antes de merge.

## Implementado
- `.github/workflows/ci.yml`
  - Nuevo job `c1-exit-gate`
  - Ejecuta contrato Playwright de Figo contra staging
  - Smoke del endpoint de chat con fallback `'/api/chat' -> '/figo-chat.php'`
  - Gate de latencia (p95 Figo <= 3000ms)
  - Integracion con `paths-filter` para skip cuando no hay cambios relevantes
  - `build` depende de `c1-exit-gate`
- `tests/figo-chat-contract.spec.js`
  - Soporte para `TEST_FIGO_CHAT_ENDPOINT_PATH` (default `/figo-chat.php`)

## Validacion local
- Parse YAML:
  - `.github/workflows/ci.yml` -> OK
- JS syntax:
  - `node --check tests/figo-chat-contract.spec.js` -> OK
  - `node --check eslint.config.js` -> OK (config relacionada al gate CI)

## Validacion remota (parcial)
- Se habilito `workflow_dispatch` en `CI` para permitir corrida manual en rama de validacion.
- Run `CI` manual: `22384721088`
  - `c1-exit-gate` fue creado en el grafo del workflow pero quedo `skipped`
  - Motivo: fallos previos en jobs existentes no relacionados (`unit-tests`, `e2e-tests`) bloquearon la cadena de `needs`

## Limite de esta evidencia
El gate C1 no se ejecuto end-to-end en remoto porque el workflow `CI` quedo rojo antes de llegar a `c1-exit-gate`.

## Pendiente externo/manual
- Ejecutar `CI` con base verde (o rerun selectivo una vez estabilizados `unit-tests`/`e2e-tests`) para confirmar corrida real de `c1-exit-gate` contra staging.
