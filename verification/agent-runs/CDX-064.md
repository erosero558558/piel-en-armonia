# CDX-064

## Resumen

Slice transversal de follow-up para aterrizar el refuerzo de reconciliacion de
tareas `ready` huerfanas y el endurecimiento de trazabilidad del board,
extraido de forma aislada desde el donor commit `01e6190c`.

## Contexto

- `CDX-061`, `CDX-062` y `CDX-063` ya quedaron cerradas en el frente
  multi-clinica.
- El donor no se publica desde su branch multi-commit; se extrajo limpio en un
  clone nuevo sobre `origin/main@6666be38`.
- El canon actual del repo ya no es multi-clinica sino
  `STRAT-2026-03-admin-shell-rc2-polish`, por eso este soporte se alinea al
  subfrente `SF-transversal-admin-shell-rc2-support`.
- El slot obsoleto `CDX-064` se reaprovecha como follow-up transversal limpio
  en vez de inventar un `CDX-*` nuevo por fuera de la numeracion ya sembrada en
  el board.

## Objetivo

- Normalizar `ready` huerfanas fuera de `strategy.active` o fuera del foco
  activo como candidatas seguras de `board sync`.
- Agregar `strategy activate-next --normalize-ready-orphans`.
- Separar `orphan_slot_tasks` de `orphan_ready_tasks` en `codex-check` y
  `status --json --explain-red`.
- Endurecer `writeBoard` y detectar `board_events_drift` cuando el ledger
  contradiga al board.
- Mantener `codex-model-usage.jsonl` con enforcement prospectivo, sin backfill
  sintetico.

## Donor

- Repo donor:
  `/Users/luciaguadalupecaizasanchez/Documents/GitHub/Aurora-Derm-cdx063-orphan-reconcile-3`
- Commit donor: `01e6190c50753b1a9b6315def88e3fe70a7ec28f`
- Mensaje donor: `feat: reconcile ready orphan tasks by active strategy`
- Commit extraido en este clone:
  `92483406 feat: reconcile ready orphan tasks by active strategy`

## Cambios aterrizados

- `board sync` pasa a detectar `ready` fuera del frente/foco como
  `normalized_candidates` seguras.
- `strategy activate-next` soporta `--normalize-ready-orphans`.
- `codex-check` separa `orphan_ready_tasks` de `orphan_slot_tasks`.
- `status --json --explain-red` separa `active_front_blockers`,
  `historical_debt` y `workspace_visibility_warnings`.
- `writeBoard` deja de silenciar fallos de append en
  `verification/agent-board-events.jsonl`.
- Se agrega chequeo estructural `board_events_drift`.
- El ledger `verification/codex-model-usage.jsonl` queda con enforcement
  prospectivo al arrancar sesiones Codex nuevas.

## Validacion ejecutada

```bash
node --test tests-node/agent-orchestrator-cli.test.js
node --test tests-node/orchestrator/board-sync-engine.test.js
node --test tests-node/orchestrator/board-events-engine.test.js
node --test tests-node/orchestrator/status-engine.test.js
node agent-orchestrator.js codex-check --json
node agent-orchestrator.js status --json --explain-red
node agent-orchestrator.js board sync check --json
```

Resultados:

- `tests-node/agent-orchestrator-cli.test.js`: `105/105` verde
- `tests-node/orchestrator/board-sync-engine.test.js`: `6/6` verde
- `tests-node/orchestrator/board-events-engine.test.js`: `4/4` verde
- `tests-node/orchestrator/status-engine.test.js`: `2/2` verde
- `codex-check --json`: `ok=true`
- `board sync check --json`: `ok=true`, sin `normalized_candidates` ni
  `blocking_findings` en el board actual
- `status --json --explain-red`: `signal=NOT_RED`, sin
  `active_front_blockers`, `historical_debt` ni
  `workspace_visibility_warnings`

## Notas operativas

- `codex start CDX-064 --block C1 --expect-rev 835 --json` se intentó para
  dejar trazabilidad por CLI, pero la CLI respondió
  `workspace_root_not_ready` porque el bootstrap manual del slot `CDX-064`
  ya había ensuciado el root con cambios in-scope del propio task.
- Se mantuvo `CDX-064` en `ready` alineada al frente actual; el follow-up queda
  validado desde ese estado porque el task no necesita consumir slot ni bloque
  `CODEX_ACTIVE` para este publish transversal.
- Persisten warnings operativos ajenos a este follow-up:
  `test:frontend:qa:admin=unverified(snapshot_missing)` del foco activo RC2 y
  `public_main_sync` en estado `unknown/unverified`.
