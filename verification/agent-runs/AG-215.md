# AG-215 - Use remote asset refs for prod cache-header verification

## Summary

- Fixed `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1` so cache-header validation uses the remote style refs published by the live page instead of local asset refs.
- Added a dedicated contract in `tests-node/prod-verify-asset-cache-contract.test.js` to prevent regressions back to local refs.

## Validation

- `node --test tests-node/prod-ops-public-sync-contract.test.js tests-node/prod-verify-asset-cache-contract.test.js`
  - Result: `5/5` passing.
- PowerShell parse validation:
  - `scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1`
- Live verify repro after fix:
  - `pwsh -File scripts/ops/prod/VERIFICAR-DESPLIEGUE.ps1 -Domain https://pielarmonia.com -AssetHashRetryCount 3 -AssetHashRetryDelaySec 6 -ReportPath verification/last-deploy-verify-ag215.json`
  - Result: `Resultado: verificacion de despliegue OK.`

## Outcome

- The previous false failure on `cache-header:critical-css` disappeared because the verifier now checks the remote `_astro/index.C5XiYOC1.css` ref actually served by production.
- This removes a misleading blocker from `repair-git-sync` / production verification and leaves the remaining blockers concentrated on real host transport / `public_main_sync` issues.
