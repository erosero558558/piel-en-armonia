version: '3.8'

services:
  app:
    build: .
    container_name: AURORADERM_app
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - AURORADERM_data:/var/www/data
      - ./env.php:/var/www/html/env.php
    environment:
      - AURORADERM_DATA_DIR=/var/www/data
      - AURORADERM_REDIS_HOST=redis
    depends_on:
      - redis
    networks:
      - AURORADERM_net

  redis:
    image: redis:alpine
    container_name: AURORADERM_redis
    restart: always
    volumes:
      - AURORADERM_redis_data:/data
    networks:
      - AURORADERM_net

  prometheus:
    image: prom/prometheus:latest
    container_name: AURORADERM_prometheus
    volumes:
      - ./prometheus.docker.yml:/etc/prometheus/prometheus.yml
      - ./prometheus.rules.yml:/etc/prometheus/prometheus.rules.yml
      - AURORADERM_prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    ports:
      - "9090:9090"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - AURORADERM_net

  grafana:
    image: grafana/grafana:latest
    container_name: AURORADERM_grafana
    ports:
      - "3000:3000"
    volumes:
      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/dashboard.json:/var/lib/grafana/dashboards/dashboard.json
      - AURORADERM_grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    restart: unless-stopped
    depends_on:
      - prometheus
    networks:
      - AURORADERM_net

volumes:
  AURORADERM_data:
  AURORADERM_redis_data:
  AURORADERM_prometheus_data:
  AURORADERM_grafana_data:

networks:
  AURORADERM_net:
    driver: bridge

