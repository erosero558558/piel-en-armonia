name: Jules PR Automation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, closed]

jobs:
  validate-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow is active
        run: echo "jules-pr workflow parsed and active"
