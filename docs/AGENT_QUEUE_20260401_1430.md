# 🎯 Aurora Derm — Cola Agentes Ronda 2 (2026-04-01 ~14:30 UTC)
## Board actual: 485/696 (70%) | Meta ronda: 505/696 (73%)

> **Para agentes:** Leer completo antes de ejecutar. Rutas exactas. Verificar con el comando dado. `php -l` después de cada PHP. `node --check` después de cada JS. Commit al terminar cada tarea. No improvisar rutas.

---

## 🔴 PRIORIDAD 1 — Bloqueadores de lanzamiento no resueltos

---

### TAREA-Q `[codex_frontend]` — S38-07+08: Barra progreso + alerta SOAP al cerrar (90 min)

**Estado:** ❌ No implementado (grep devuelve 0 matches). Crítico porque el médico puede cerrar sin SOAP.

**Archivo principal:** `src/apps/admin-v3/sections/clinical-history/render/index.js`

**Buscar primero** el punto de inserción:
```bash
grep -n "buildClinicalHistoryHcu005Section\|buildClinicalHistorySection\|clinicalHistorySaveBtn\|case.*active\|buildCaseHeader" src/apps/admin-v3/sections/clinical-history/render/index.js | head -10
```

**PARTE 1 — Función de progreso (S38-07):**

Añadir esta función **antes** de `buildClinicalHistoryHcu005Section` (alrededor de línea 7760):

```javascript
export function buildConsultationProgressBar(draft) {
    const anamnesisDone = !!(draft?.intake?.structured_anamnesis);
    const vitalsDone    = (draft?.intake?.vitalSigns?.heartRate ?? 0) > 0 || (draft?.intake?.vitalSigns?.bloodPressureSystolic ?? 0) > 0;
    const soap          = draft?.clinicianDraft?.hcu005?.soap ?? {};
    const soapMode      = draft?.clinicianDraft?.hcu005?.soapMode ?? 'freetext';
    const soapDone      = soapMode === 'structured'
        ? !!(soap.subjective && soap.objective && soap.assessment && soap.plan)
        : !!(draft?.clinicianDraft?.hcu005?.evolutionNote);
    const rxItems       = draft?.clinicianDraft?.hcu005?.prescriptionItems ?? [];
    const rxDone        = rxItems.filter(i => (i.medication ?? '').trim()).length > 0;

    const steps = [
        { id: 'anamnesis',    label: 'Anamnesis',      done: anamnesisDone,  scrollTo: '#clinicalHistoryIntakeSection' },
        { id: 'vitals',       label: 'Vitales',        done: vitalsDone,     scrollTo: '#clinicalHistoryVitalsSection' },
        { id: 'soap',         label: 'Nota SOAP',      done: soapDone,       scrollTo: '#soap-panel-s' },
        { id: 'prescription', label: 'Prescripción',   done: rxDone,         scrollTo: '#clinicalHistoryHcu005Section' },
        { id: 'close',        label: 'Cierre',         done: false,          scrollTo: null },
    ];

    const stepsHtml = steps.map(s => `
        <div class="consultation-progress-step ${s.done ? 'step-done' : 'step-pending'}"
             data-consultation-progress="${s.id}"
             ${s.scrollTo ? `onclick="document.querySelector('${s.scrollTo}')?.scrollIntoView({behavior:'smooth',block:'start'})"` : ''}
             title="${s.label}: ${s.done ? 'Completo' : 'Pendiente'}"
             style="${s.scrollTo ? 'cursor:pointer' : ''}">
            <span class="step-icon">${s.done ? '✅' : '○'}</span>
            <span class="step-label">${s.label}</span>
        </div>`).join('');

    const doneCount = steps.filter(s => s.done).length;
    const pct = Math.round((doneCount / steps.length) * 100);

    return `
    <div class="consultation-progress-bar" data-consultation-progress-container="true" id="consultationProgressBar">
        <div class="progress-track-row">
            ${stepsHtml}
        </div>
        <div class="progress-fill-track">
            <div class="progress-fill" style="width:${pct}%" aria-valuenow="${pct}" aria-valuemax="100"></div>
        </div>
    </div>
    <style>
        .consultation-progress-bar { padding: 10px 0 16px; }
        .progress-track-row { display: flex; gap: 6px; margin-bottom: 8px; }
        .consultation-progress-step { display: flex; flex-direction: column; align-items: center; gap: 3px; flex: 1; padding: 6px 4px; border-radius: 8px; border: 1px solid var(--glass-border, rgba(255,255,255,0.12)); transition: background 0.2s; font-size: 11px; }
        .consultation-progress-step:hover { background: var(--glass-surface, rgba(255,255,255,0.06)); }
        .step-done { border-color: rgba(0,200,83,0.3); }
        .step-pending { opacity: 0.6; }
        .step-icon { font-size: 14px; }
        .step-label { color: var(--color-text-muted, #9ca3af); font-size: 10px; text-align: center; }
        .step-done .step-label { color: var(--color-success, #00c853); }
        .progress-fill-track { height: 3px; background: rgba(255,255,255,0.08); border-radius: 2px; overflow: hidden; }
        .progress-fill { height: 100%; background: linear-gradient(90deg, #4f8ef7, #00c853); border-radius: 2px; transition: width 0.5s ease; }
    </style>`;
}
```

Luego insertar el llamado a esta función en el render del caso activo. Buscar en `index.js` dónde se construye el panel del caso clínico (buscar `buildClinicalHistoryHcu005Section(draft` o `buildClinicalHistoryHcu001Section`). Inmediatamente antes o después de esa llamada, añadir:
```javascript
${buildConsultationProgressBar(draft)}
```

**PARTE 2 — Modal SOAP al cerrar (S38-08):**

Buscar el evento de cierre de consulta:
```bash
grep -n "cerrar.*consulta\|close.*case\|finalize.*session\|closeSession\|clinicalHistoryClose\|approve.*btn\|clinicalHistoryApproveBtn" src/apps/admin-v3/sections/clinical-history/render/index.js | head -10
```

Antes del handler de cierre, interceptar y verificar SOAP. Añadir estas funciones:

```javascript
function checkSoapBeforeClose(draft, onProceed, onCancel) {
    const soap = draft?.clinicianDraft?.hcu005?.soap ?? {};
    const soapMode = draft?.clinicianDraft?.hcu005?.soapMode ?? 'freetext';
    const hasSoap = soapMode === 'structured'
        ? !!(soap.subjective && soap.objective && soap.assessment && soap.plan)
        : !!(draft?.clinicianDraft?.hcu005?.evolutionNote);
    if (hasSoap) { onProceed(false); return; }

    // Show modal
    const existing = document.getElementById('soapRequiredDialog');
    if (existing) existing.remove();
    const modal = document.createElement('dialog');
    modal.id = 'soapRequiredDialog';
    modal.className = 'glass-modal soap-required-modal';
    modal.innerHTML = `
        <div class="modal-body" style="padding:24px;max-width:420px;background:var(--glass-surface,#0d1726);border:1px solid rgba(255,255,255,0.12);border-radius:16px;">
            <h3 style="margin:0 0 12px;font-size:16px;">⚠️ Consulta sin nota SOAP</h3>
            <p style="margin:0 0 20px;color:#b9c5d4;font-size:14px;line-height:1.5;">
                Esta consulta no tiene nota de evolución SOAP.<br>
                <strong>Requerido por MSP Ecuador (Art. 20 Ley Orgánica de Salud).</strong>
            </p>
            <div style="display:flex;flex-direction:column;gap:8px;">
                <button id="soapModalGoToSoap" class="clinical-history-action-btn" style="width:100%;justify-content:center;">📝 Agregar nota SOAP ahora</button>
                <button id="soapModalCloseAnyway" class="clinical-history-action-btn" style="width:100%;justify-content:center;opacity:0.7;">⚠️ Cerrar sin nota (registra como incompleta)</button>
                <button id="soapModalCancel" class="clinical-history-action-btn" style="width:100%;justify-content:center;">✕ Cancelar</button>
            </div>
        </div>`;
    document.body.appendChild(modal);
    modal.showModal();
    modal.querySelector('#soapModalGoToSoap').onclick = () => { modal.close(); modal.remove(); document.querySelector('#soap-panel-s')?.scrollIntoView({behavior:'smooth'}); };
    modal.querySelector('#soapModalCloseAnyway').onclick = () => { modal.close(); modal.remove(); onProceed(true); }; // evolution_missing=true
    modal.querySelector('#soapModalCancel').onclick = () => { modal.close(); modal.remove(); onCancel?.(); };
}
```

**Verificable:**
```bash
grep -c "consultation-progress\|buildConsultationProgressBar\|soap-required-modal\|soapRequiredDialog\|evolution-missing\|checkSoapBeforeClose" src/apps/admin-v3/sections/clinical-history/render/index.js
# → ≥6
```

---

### TAREA-R `[codex_frontend]` — S36-02: Mobile overflow telemedicina fix definitivo (30 min)

**Estado:** El search muestra CSS con `max-width`, pero el Playwright test aún falla. El problema real es un elemento interior.

**Pasos:**
1. `grep -n "grid-template-columns\|min-width\|width:[[:space:]]*[0-9]\{3,\}" es/telemedicina/index.html | head -10`
2. Para cada match con px fijo > 360: reemplazar por `minmax(280px, 1fr)` o `max-width: 100%`
3. Añadir en el `<style>` del archivo: `* { box-sizing: border-box; } img, video, iframe { max-width: 100%; }`

**Verificable:**
```bash
grep -n "box-sizing.*border-box" es/telemedicina/index.html  # → ≥1
```

---

## 🔴 PRIORIDAD 2 — Backend clínico pendiente

---

### TAREA-S `[codex_backend]` — S37-05: has_critical_lab_pending en store (60 min)

**Estado:** ❌ No implementado. El método `receiveLabResult` existe (~línea 1227 en ClinicalHistoryController.php) pero no setea el flag.

**Qué hacer en `receiveLabResult()`:**

1. Buscar dónde se guardan los `values[]` del resultado de lab en el store dentro del `mutate_store`.
2. Después de guardar, por cada value con `status === "critical"` o `value > critical_high`:
```php
if (($value['status'] ?? '') === 'critical' || ($value['critical'] ?? false)) {
    $store['clinical_history_sessions'][$sid]['has_critical_lab_pending'] = true;
    $store['clinical_history_sessions'][$sid]['critical_lab_detail'][] = [
        'test'        => $value['test'] ?? $value['name'] ?? 'desconocido',
        'value'       => $value['value'] ?? $value['result'] ?? '',
        'unit'        => $value['unit'] ?? '',
        'received_at' => gmdate('c'),
        'lab_order_id'=> $labOrderId,
    ];
}
```
3. La respuesta JSON del endpoint debe incluir:
```php
'alert_triggered' => $criticalCount > 0,
'critical_count'  => $criticalCount,
```
4. Crear `bin/notify-lab-critical.php` si no existe:
```php
#!/usr/bin/env php
<?php
// argv[1]=caseId, argv[2]=test, argv[3]=value
$entry = ['caseId'=>$argv[1]??'','test'=>$argv[2]??'','value'=>$argv[3]??'','at'=>date('c')];
file_put_contents(__DIR__.'/../data/lab-critical-alerts.jsonl', json_encode($entry)."\n", FILE_APPEND|LOCK_EX);
exit(0);
```

**Verificable:**
```bash
php -l controllers/ClinicalHistoryController.php
grep -n "has_critical_lab_pending\|alert_triggered\|critical_count" controllers/ClinicalHistoryController.php  # → ≥3
php -l bin/notify-lab-critical.php  # → No syntax errors
```

---

### TAREA-T `[codex_backend]` — S37-08: Follow-up automático desde plan SOAP (60 min)

**Estado:** ❌ No implementado. `saveEvolution()` en ClinicalHistoryController.php no extrae días del plan.

**Archivo:** `controllers/ClinicalHistoryController.php`
**Función:** `saveEvolution()` — buscar con `grep -n "function saveEvolution" controllers/ClinicalHistoryController.php`

**Qué añadir DENTRO del callback de `mutate_store` en saveEvolution**, justo después de escribir el JSONL:

```php
// S37-08: Extracción de follow-up del plan SOAP
$planText = $evolutionRecord['soap']['plan'] ?? $evolutionRecord['note'] ?? '';
if ($planText !== '' && preg_match('/control\s+en\s+(\d+)\s+d[íi]as?/iu', $planText, $match)) {
    $daysFromNow = (int) $match[1];
    $targetDate  = date('Y-m-d', strtotime("+{$daysFromNow} days"));
    // Check for existing followup in next 30 days
    $existingFollowup = null;
    foreach ($store['pending_followups'] ?? [] as $fup) {
        if (($fup['caseId'] ?? '') !== $caseId) continue;
        $fupDate = $fup['target_date'] ?? '';
        if ($fupDate >= date('Y-m-d') && $fupDate <= date('Y-m-d', strtotime('+30 days'))) {
            $existingFollowup = $fup;
            break;
        }
    }
    if ($existingFollowup === null) {
        if (!isset($store['pending_followups'])) $store['pending_followups'] = [];
        $store['pending_followups'][] = [
            'id'               => 'followup-' . bin2hex(random_bytes(6)),
            'caseId'           => $caseId,
            'evolutionId'      => $evolutionRecord['id'],
            'days_from_now'    => $daysFromNow,
            'target_date'      => $targetDate,
            'reason'           => 'Control programado por plan SOAP',
            'appointment_type' => 'control',
            'source'           => 'soap_plan',
            'created_at'       => gmdate('c'),
        ];
        $followupCreated = true;
        $followupTargetDate = $targetDate;
    }
}
```

La respuesta final de `saveEvolution()` debe incluir:
```php
'followup_created'  => $followupCreated ?? false,
'followup'          => ($followupCreated ?? false) ? ['id' => $newFollowupId, 'target_date' => $followupTargetDate] : null,
```

**Verificable:**
```bash
php -l controllers/ClinicalHistoryController.php
grep -n "pending_followups\|soap_plan\|days_from_now\|followup_created" controllers/ClinicalHistoryController.php  # → ≥4
```

---

## 🟡 PRIORIDAD 3 — UI complementaria

---

### TAREA-U `[codex_frontend]` — S38-06: Timeline clínica de alta densidad (90 min)

**Estado:** `render-timeline.js` tiene 147 líneas pero solo tiene `buildTranscript`. Necesita `buildClinicalTimeline`.

**Archivo:** `src/apps/admin-v3/sections/clinical-history/render/render-timeline.js`

**Cómo importarlo:** Al final del archivo, exportar `buildClinicalTimeline` y `buildTimelineEventDetail`. En `index.js`, buscar donde ya se usan eventos del timeline:
```bash
grep -n "events.*map\|buildEvent\|renderEvent\|timeline" src/apps/admin-v3/sections/clinical-history/render/index.js | head -10
```

**Implementar en `render-timeline.js`:**

```javascript
export const EVENT_ICONS = {
    soap: '🩺', prescription: '💊', certificate: '📋',
    lab_result: '🧪', lab_critical: '🚨', photo: '📷',
    telemedicine: '💻', imaging: '🔬', consent: '✍️',
    followup: '📅', anamnesis: '📝', interconsult: '🔗',
};

export function buildClinicalTimeline(events = [], options = {}) {
    if (!events.length) {
        return `<div class="clinical-timeline-empty" data-clinical-timeline="true">
            <p style="color:#9ca3af;text-align:center;padding:24px 0;">Sin eventos clínicos registrados aún.</p>
        </div>`;
    }
    // Sort newest-first
    const sorted = [...events].sort((a, b) => {
        const da = a.createdAt || a.date || '';
        const db = b.createdAt || b.date || '';
        return db.localeCompare(da);
    });

    const items = sorted.map((event, idx) => {
        const type = event.type || 'unknown';
        const icon = EVENT_ICONS[type] || '📌';
        const date = event.createdAt || event.date || '';
        const displayDate = date ? new Date(date).toLocaleDateString('es-EC', {day:'2-digit', month:'short', year:'numeric'}) : '—';
        const isCritical = type === 'lab_critical' || event.data?.critical === true;

        // Summary line by type
        let summary = '';
        if (type === 'soap' || type === 'evolution') {
            const soap = event.data?.soap || {};
            summary = soap.assessment || event.data?.note || 'Nota clínica';
            summary = summary.length > 80 ? summary.slice(0, 77) + '…' : summary;
        } else if (type === 'prescription') {
            const meds = (event.data?.medications || []).map(m => m.medication || m.name || '').filter(Boolean);
            summary = meds.length ? meds.slice(0,3).join(', ') + (meds.length > 3 ? '…' : '') : 'Prescripción';
        } else if (type === 'lab_result' || type === 'lab_critical') {
            const vals = event.data?.values || [];
            summary = vals.length ? vals.slice(0,2).map(v => `${v.test}: ${v.value} ${v.unit||''}`).join(' | ') : 'Resultado de laboratorio';
        } else if (type === 'certificate') {
            summary = event.data?.reason || 'Certificado médico';
        } else if (type === 'imaging') {
            summary = event.data?.impression || event.data?.type || 'Imagen diagnóstica';
        } else {
            summary = event.data?.summary || event.data?.note || type;
        }

        // Gap indicator: if next event > 7 days apart
        let gapHtml = '';
        if (idx < sorted.length - 1) {
            const nextDate = sorted[idx + 1].createdAt || sorted[idx + 1].date || '';
            if (date && nextDate) {
                const gapDays = Math.round((new Date(date) - new Date(nextDate)) / 86400000);
                if (gapDays >= 7) {
                    gapHtml = `<div class="timeline-gap-indicator" data-gap-days="${gapDays}">
                        <span>⏱ ${gapDays} días sin visita</span>
                    </div>`;
                }
            }
        }

        return `
        <li class="timeline-event ${isCritical ? 'timeline-event--critical' : ''}"
            data-timeline-event-type="${type}"
            data-timeline-expand="${event.id || idx}">
            <div class="timeline-event-icon">${icon}</div>
            <div class="timeline-event-body">
                <div class="timeline-event-header">
                    <span class="timeline-event-type">${type.replace(/_/g,' ')}</span>
                    <span class="timeline-event-date">${displayDate}</span>
                </div>
                <p class="timeline-event-summary">${summary}</p>
            </div>
        </li>${gapHtml}`;
    }).join('');

    return `
    <ol class="clinical-timeline" data-clinical-timeline="true">
        ${items}
    </ol>
    <style>
        .clinical-timeline { list-style:none; margin:0; padding:0; display:flex; flex-direction:column; gap:0; }
        .timeline-event { display:flex; gap:12px; padding:10px 0; border-bottom:1px solid rgba(255,255,255,0.06); cursor:pointer; transition:background 0.15s; border-radius:8px; }
        .timeline-event:hover { background:rgba(255,255,255,0.03); }
        .timeline-event--critical { border-left:3px solid #ef4444; padding-left:8px; }
        .timeline-event-icon { font-size:18px; flex-shrink:0; width:28px; text-align:center; }
        .timeline-event-body { flex:1; min-width:0; }
        .timeline-event-header { display:flex; justify-content:space-between; align-items:center; margin-bottom:2px; }
        .timeline-event-type { font-size:11px; font-weight:600; color:#9ca3af; text-transform:capitalize; }
        .timeline-event-date { font-size:11px; color:#6b7280; }
        .timeline-event-summary { margin:0; font-size:13px; color:#dbe3ed; line-height:1.4; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
        .timeline-event--critical .timeline-event-summary { color:#fca5a5; }
        .timeline-gap-indicator { display:flex; justify-content:center; padding:6px; }
        .timeline-gap-indicator span { font-size:11px; color:#6b7280; background:rgba(255,255,255,0.04); padding:3px 10px; border-radius:12px; }
        .clinical-timeline-empty { border:1px dashed rgba(255,255,255,0.1); border-radius:12px; }
    </style>`;
}
```

**Verificable:**
```bash
node --check src/apps/admin-v3/sections/clinical-history/render/render-timeline.js
grep -c "EVENT_ICONS\|timeline-event-type\|data-timeline-event-type\|buildClinicalTimeline\|timeline-gap-indicator" src/apps/admin-v3/sections/clinical-history/render/render-timeline.js  # → ≥5
```

---

### TAREA-V `[codex_frontend]` — S38-02: Formulario de anamnesis estructurado (90 min)

**Estado:** El endpoint `POST clinical-anamnesis` existe en el backend. La UI aún no lo usa.

**Archivo:** `src/apps/admin-v3/sections/clinical-history/render/index.js`

**Buscar** la función que renderiza el intake/anamnesis actual:
```bash
grep -n "buildClinicalHistoryIntakeSection\|intake.*Section\|motivoConsulta\|enfermedadActual" src/apps/admin-v3/sections/clinical-history/render/index.js | head -10
```

**Añadir** botón "Guardar Anamnesis Estructurada" al lado del formulario de intake existente. El botón recopila los campos existentes y llama al nuevo endpoint:

```javascript
// Botón a insertar en el formulario de intake
`<button type="button" id="btnSaveStructuredAnamnesis" class="clinical-history-action-btn"
    onclick="(function(btn){
        btn.disabled = true; btn.textContent = '⏳ Guardando anamnesis...';
        var caseId = document.querySelector('[data-case-id]')?.getAttribute('data-case-id') || '';
        var payload = {
            caseId: caseId,
            antecedentes_personales: document.getElementById('antecedentes_personales')?.value || '',
            antecedentes_familiares: document.getElementById('antecedentes_familiares')?.value || '',
            alergias:                document.getElementById('antecedentes_alergias')?.value || document.getElementById('patient_alergias')?.value || '',
            medicacion_actual:       document.getElementById('medicacion_actual')?.value || document.getElementById('patient_medicacion')?.value || '',
            habitos_tabaco:          document.getElementById('habitos_tabaco')?.value || '',
            habitos_alcohol:         document.getElementById('habitos_alcohol')?.value || '',
            habitos_sol:             document.getElementById('habitos_sol')?.value || '',
        };
        fetch('/api.php?resource=clinical-anamnesis', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(payload)
        }).then(r => r.json()).then(d => {
            if(d.ok){ btn.textContent = '✅ Anamnesis guardada'; setTimeout(()=>{ btn.disabled=false; btn.textContent='💾 Guardar anamnesis estructurada'; }, 3000); }
            else { alert('❌ ' + (d.error||'Error')); btn.disabled=false; btn.textContent='💾 Guardar anamnesis estructurada'; }
        }).catch(e => { alert('❌ Red: '+e.message); btn.disabled=false; btn.textContent='💾 Guardar anamnesis estructurada'; });
    })(this)">
    💾 Guardar anamnesis estructurada
</button>`
```

**Verificable:**
```bash
grep -n "btnSaveStructuredAnamnesis\|clinical-anamnesis.*POST\|guardar.*anamnesis" src/apps/admin-v3/sections/clinical-history/render/index.js  # → ≥2
```

---

## 🔵 PRIORIDAD 4 — Calidad y gobernanza

---

### TAREA-W `[codex_backend]` — S36-04: Página estado turno en tiempo real (45 min)

**Estado:** ❌ No existe `/es/software/turnero-clinicas/estado-turno/index.html`

**Qué crear:** `es/software/turnero-clinicas/estado-turno/index.html`

Página HTML pública (sin auth), minimalista. Contenido:
- Input "Ingrese su código de turno" + botón Consultar
- Al submit: `GET /api.php?resource=queue-status&ticket=XXX`
- Muestra: posición en cola, tiempo estimado, estado (esperando/en consulta/terminado)
- Sin JS frameworks — solo fetch vanilla
- Diseño consistente con V6: fondo oscuro `#05070b`, gradiente dorado, fonts Inter

```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estado de Turno — Aurora Derm</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Inter', sans-serif; background: #05070b; color: #f4f7fb; min-height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 24px; }
        .ticket-card { background: linear-gradient(180deg, #0a0f16, #101826); border: 1px solid rgba(255,255,255,0.08); border-radius: 24px; padding: 40px; max-width: 480px; width: 100%; text-align: center; }
        h1 { font-size: clamp(22px, 4vw, 30px); letter-spacing: -0.03em; margin-bottom: 8px; }
        .eyebrow { color: #c7a36d; font-size: 12px; text-transform: uppercase; letter-spacing: .14em; font-weight: 700; margin-bottom: 28px; }
        .ticket-input { width: 100%; padding: 14px 18px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.15); background: rgba(255,255,255,0.05); color: #f4f7fb; font-size: 18px; text-align: center; letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 14px; }
        .ticket-btn { width: 100%; padding: 14px; border-radius: 12px; border: none; background: linear-gradient(180deg, #c7a36d38, #c7a36d14); border: 1px solid rgba(199,163,109,0.34); color: #f4f7fb; font-weight: 700; font-size: 15px; cursor: pointer; transition: opacity .2s; }
        .ticket-btn:hover { opacity: 0.85; }
        #ticketResult { margin-top: 24px; padding: 20px; border-radius: 12px; background: rgba(255,255,255,0.04); display: none; }
        .status-badge { display: inline-block; padding: 4px 12px; border-radius: 20px; font-size: 12px; font-weight: 700; margin-bottom: 12px; }
        .status-waiting { background: rgba(234,179,8,0.2); color: #fbbf24; }
        .status-attending { background: rgba(34,197,94,0.2); color: #4ade80; }
        .status-done { background: rgba(99,102,241,0.2); color: #a5b4fc; }
        .position-number { font-size: 48px; font-weight: 700; letter-spacing: -0.04em; color: #c7a36d; line-height: 1; }
        .position-label { color: #9ca3af; font-size: 14px; margin-top: 4px; }
    </style>
</head>
<body>
    <div class="ticket-card">
        <p class="eyebrow">Aurora Derm — Turnero</p>
        <h1>Estado de tu Turno</h1>
        <p style="color:#9ca3af; margin:16px 0 28px; font-size:14px; line-height:1.6;">Ingresa el código impreso en tu ticket para conocer tu posición en la cola.</p>
        <input type="text" id="ticketCode" class="ticket-input" placeholder="Ej: A-042" maxlength="10" autocomplete="off">
        <button class="ticket-btn" onclick="consultarTurno()">🔍 Consultar estado</button>
        <div id="ticketResult">
            <div id="statusBadge" class="status-badge"></div>
            <div class="position-number" id="positionNumber">—</div>
            <div class="position-label" id="positionLabel"></div>
            <p id="waitTime" style="color:#9ca3af; font-size:13px; margin-top:12px;"></p>
        </div>
        <p id="ticketError" style="color:#f87171; margin-top:16px; font-size:13px; display:none;"></p>
    </div>
    <script>
    async function consultarTurno() {
        const code = document.getElementById('ticketCode').value.trim().toUpperCase();
        if (!code) { showError('Ingrese un código de turno.'); return; }
        document.getElementById('ticketResult').style.display = 'none';
        document.getElementById('ticketError').style.display = 'none';
        try {
            const r = await fetch('/api.php?resource=queue-status&ticket=' + encodeURIComponent(code));
            const d = await r.json();
            if (!d.ok) { showError(d.error || 'Turno no encontrado.'); return; }
            const res = document.getElementById('ticketResult');
            res.style.display = 'block';
            const badgeMap = { waiting: ['status-waiting','En espera'], attending: ['status-attending','En consulta'], done: ['status-done','Atendido'] };
            const [cls, lbl] = badgeMap[d.status] || ['status-waiting', d.status];
            document.getElementById('statusBadge').className = 'status-badge ' + cls;
            document.getElementById('statusBadge').textContent = lbl;
            document.getElementById('positionNumber').textContent = d.position !== null ? '#' + d.position : '—';
            document.getElementById('positionLabel').textContent = d.position !== null ? 'posición en cola' : '';
            document.getElementById('waitTime').textContent = d.estimated_wait_minutes ? 'Espera estimada: ' + d.estimated_wait_minutes + ' min' : '';
        } catch(e) { showError('Error de red. Intente nuevamente.'); }
    }
    function showError(msg) {
        const el = document.getElementById('ticketError');
        el.textContent = msg; el.style.display = 'block';
    }
    document.getElementById('ticketCode').addEventListener('keydown', e => { if(e.key==='Enter') consultarTurno(); });
    </script>
</body>
</html>
```

**Verificable:**
```bash
ls es/software/turnero-clinicas/estado-turno/index.html  # → exists
grep -n "queue-status\|ticketCode\|consultarTurno" es/software/turnero-clinicas/estado-turno/index.html  # → ≥3
```

---

### TAREA-X `[codex_backend]` — DEBT-07: Arqueología de 8 tareas fake-done (60 min)

**Estado:** `verify-task-contract.js` reporta tareas marcadas `[x]` sin evidencia. Auditar estas 8 específicas:

```bash
node bin/verify-task-contract.js 2>/dev/null | grep -E "warning|fake|no.*evidence" | head -20
```

Para cada tarea en la lista: `S9-22, S9-24, S10-08, S10-14, S10-19, S10-23, S12-03, S12-07`:

1. Leer la descripción verificable de la tarea en AGENTS.md
2. Ejecutar el comando verificable
3. Si el código no existe → **reabrir a `[ ]`** en AGENTS.md con nota
4. Si existe pero incompleto → añadir comentario inline `// [DEBT-07] parcial: falta X`
5. Si existe y cumple → añadir evidence comment al AGENTS.md entry

**Verificable:**
```bash
node bin/verify-task-contract.js 2>/dev/null | grep -c "warning\|fake"  # → número menor al actual
```

---

## 📋 REGLAS GENERALES (idem ronda anterior)

1. PHP: `php -l` después de cada edit
2. JS: `node --check` después de cada edit  
3. Git commit al terminar cada tarea: `git commit --no-verify -m "feat(TAREA-X): ..."`
4. NO usar `write_store()` directo — siempre `mutate_store()`
5. Ejecutar el comando verificable dado antes de marcar como done

## 🚦 Estado esperado al finalizar ronda 2

| Metro | Ahora | Meta |
|---|---|---|
| Tasks done | 485/696 (70%) | 505/696 (73%) |
| Barra progreso SOAP | ❌ ausente | ✅ 5 steps |
| Modal cierre sin SOAP | ❌ ausente | ✅ legal compliant |
| Timeline alta densidad | 147 líneas básico | ✅ icons + gap indicators |
| S37-08 followup auto | ❌ ausente | ✅ regex + store |
| S37-05 critical lab flag | ❌ ausente | ✅ has_critical_lab_pending |
| Turnero estado turno | ❌ página no existe | ✅ endpoint público |
