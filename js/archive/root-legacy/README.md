## Root Legacy JS Archive

These files are historical copies of root-level runtime artifacts that are not part of the active public runtime or the admin shell anymore.

Rules:

- Keep them only as archive references.
- Do not restore them to the repo root by inertia.
- Active runtime JS must live in `js/engines/**` or another documented
  canonical surface.

Archived files:

- `booking-engine.js`
- `utils.js`
